# Autopilot 사용자 가이드 ↔ 코드 구현 일치 여부 보고서

> 작성일: 2026-07-05  
> 대상: `autopilot_user_guide.md` (커밋 8a3bf7e+)  
> 기준 코드: `/home/betaflight/autopilot/src/main/`

---

## 1. 개요

`autopilot_user_guide.md`에 기술된 모든 주요 기능이 **소스 코드에 실제로 구현되어 있음**을 확인했습니다.  
원본 Betaflight 저장소(`/home/betaflight/betaflight/`)와 비교하여 Autopilot 브랜치에만 존재하는 독자적인 기능들이 다수 포함되어 있습니다.

---

## 2. 기능별 구현 상태

### 2.1 Waypoint Mission 시스템 (FlightPlan)

| 가이드 항목 | 구현 상태 | 구현 파일 | 비고 |
|---|---|---|---|
| 최대 15개 Waypoint | ✅ 구현 완료 | `flight/mission.h:25` (`MAX_MISSION_WAYPOINTS = 15`) | |
| Waypoint 타입 8종 | ✅ 구현 완료 | `flight/mission.h:27-36` (`FLYOVER`, `FLYBY`, `HOLD`, `LAND`, `TAKEOFF`, `ALT_CHANGE`, `DELAY`, `YAW_RATE`) | |
| 위치/고도/속도 저장 | ✅ 구현 완료 | `flight/mission.h:43-51` (`missionWaypoint_t` 구조체) | |
| FlightPlan 탭 연동 | ✅ 구현 완료 | `cli/cli.c:4951-5136` (CLI `waypoint` 명령어) | Configurator → CLI 명령어 변환 |
| EEPROM 저장 (PG) | ✅ 구현 완료 | `pg/mission.c` (`PG_REGISTER_WITH_RESET_TEMPLATE`) | `save` 명령 시 Flash 저장 |
| Waypoint 완료 후 Home 귀환 | ✅ 구현 완료 | `flight/mission.c:104-121` (`missionStop()`) | GPS_FIX_HOME → FLY_HOME, 미설정 → SHUTTLE_INFINITE |

### 2.2 AUX 스위치 3-Way 모드 전환

| 가이드 항목 | 구현 상태 | 비고 |
|---|---|---|
| `< 1400` = 셔틀 (무한 왕복) | ✅ 구현 완료 | `flight/gps_rescue.c:1198-1208` - 원본 BF는 `< 1500` 단일 분기 |
| `1400 ~ 1600` = Autopilot | ✅ 구현 완료 | `flight/gps_rescue.c:1203-1204` - `USE_FLIGHT_PLAN` 조건부 |
| `> 1600` = Rescue | ✅ 구현 완료 | `flight/gps_rescue.c:1207` |
| 실시간 모드 전환 가능 | ✅ 구현 완료 | `flight/gps_rescue.c:1221-1224` - AUX 탈출 체크, `gps_rescue.c:1441-1454` - SHUTTLE/FLY_HOME 단계에서도 전환 가능 |

### 2.3 OSD 표시

| 가이드 항목 | 구현 상태 | 비고 |
|---|---|---|
| `AUTO-1/4` (미션 진행) | ✅ 구현 완료 | `osd/osd_elements.c:1098-1106` - `#ifdef USE_FLIGHT_PLAN` |
| `FLY HOME` | ✅ 구현 완료 | `osd/osd_elements.c:1118-1119` |
| `CLIMB` | ✅ 구현 완료 | `osd/osd_elements.c:1115-1116` |
| `DESCEND` | ✅ 구현 완료 | `osd/osd_elements.c:1131-1132` |
| `SHUT-001` (셔틀) | ✅ 구현 완료 | `osd/osd_elements.c:1122-1123` |
| GPS 좌표 레이블 `W` | ✅ 구현 완료 | `flight/gps_rescue.c:1520-1524` (`gpsRescueGetTargetLabel()`) |
| GPS 좌표 레이블 `A`/`B` | ✅ 구현 완료 | `flight/gps_rescue.c:1525-1526` |
| GPS 좌표 레이블 `H` | ✅ 구현 완료 | `flight/gps_rescue.c:1534` |
| `READY` (아밍 준비) | ✅ 구현 완료 | `osd/osd_elements.c:1093-1095` - `BOXREADY` 모드 |
| `osd_ready_mode_pos` CLI | ✅ 구현 완료 | `cli/settings.c:1452` |

### 2.4 셔틀(Shuttle) 모드

| 가이드 항목 | 구현 상태 | 비고 |
|---|---|---|
| A-B 포인트 무한 왕복 | ✅ 구현 완료 | `flight/gps_rescue.c:407-470` (`initShuttlePoints()`) |
| CPA 기반 터치 판정 | ✅ 구현 완료 | `flight/gps_rescue.c:526-567` (`handleShuttleProgress()`) |
| Home fix 유/무에 따른 분기 | ✅ 구현 완료 | Home fix O → 홈 방향 A-B, Home fix X → 헤딩 기준 A-B |
| OSD 셔틀 왕복 횟수 표시 | ✅ 구현 완료 | `gpsRescueGetCurrentShuttleTrips()` |

### 2.5 Pre-arm Wiggle (이륙 전 서보 알림)

| 가이드 항목 | 구현 상태 | 비고 |
|---|---|---|
| `ready_to_arm_wiggle_hz` (0-6Hz) | ✅ 구현 완료 | `flight/servos.c:735-777` |
| 에일러론 위글 | ✅ 구현 완료 | `flight/servos.c:837` - Roll input에 offset 주입 |
| GPS 조건에 따른 이중 위글 | ✅ 구현 완료 | `flight/servos.c:751-761` - GPS fix 상태에 따라 Phase 분기 |
| 조종간 입력 시 중단 | ✅ 구현 완료 | `flight/servos.c:740-744` - `isArmStickActive()` 체크 |
| 기본값 4Hz | ✅ 구현 완료 | `flight/servos.c:71` (`servoConfig()->ready_to_arm_wiggle_hz = 4`) |

### 2.6 안전 기능

| 가이드 항목 | 구현 상태 | 비고 |
|---|---|---|
| Waypoint 타임아웃 (5분) | ✅ 구현 완료 | `flight/mission.c:56` (`MISSION_WP_TIMEOUT_US = 300000000`) |
| Home 미설정 시 마지막 WP 선회 | ✅ 구현 완료 | `flight/mission.c:118-120` - `RESCUE_SHUTTLE_INFINITE` |
| GPS 손실 시 안전 대응 | ✅ 구현 완료 | `flight/gps_rescue.c` - `performSanityChecks()` |
| 무장(ARMED) 중 CLI 차단 | ✅ 구현 완료 | `cli/cli.c:5002-5004, 5010-5011` - `ARMING_FLAG(ARMED)` 체크 |
| `waypoint list`만 허용 | ✅ 구현 완료 | `cli/cli.c:4974-5000` |

### 2.7 CLI 명령어

| 가이드 항목 | 구현 상태 | 비고 |
|---|---|---|
| `waypoint list` | ✅ 구현 완료 | `cli/cli.c:4974` |
| `waypoint clear` | ✅ 구현 완료 | `cli/cli.c:5001` |
| `waypoint insert` | ✅ 구현 완료 | `cli/cli.c:5008` |
| `waypoint update` | ✅ 구현 완료 | `cli/cli.c:5069` |
| `waypoint remove` | ✅ 구현 완료 | `cli/cli.c:5114` |
| `waypoint status` | ✅ 구현 완료 | `cli/cli.c:5125` |
| `diff` / `dump` 미표시 | ✅ 확인 | CLI `waypoint list`로만 확인 가능 |

---

## 3. 원본 Betaflight 대비 변경/추가 사항 요약

| 구분 | 원본 Betaflight | Autopilot 브랜치 |
|---|---|---|
| `flight/mission.c/h` | ❌ 없음 | ✅ 신규 파일 (Waypoint 미션 관리) |
| `pg/mission.c/h` | ❌ 없음 | ✅ 신규 파일 (EEPROM 저장) |
| `USE_FLIGHT_PLAN` | ❌ 미정의 | ✅ 조건부 컴파일 정의 |
| `cli/cli.c` `cliWaypoint()` | ❌ 없음 | ✅ `#ifdef USE_FLIGHT_PLAN` 블록 |
| `gps_rescue.c` 구조체 | 지역(`static`) 정의 | 헤더(`gps_rescue.h`)로 이동 + `mission.c`에서 접근 가능 |
| `gps_rescueStart/Stop()` | `static` | 전역(`extern`) 함수로 변경 |
| AUX 분기 (셔틀/Autopilot/Rescue) | `< 1500` / `>= 1500` 2-way | `< 1400` / `< 1600` / `>= 1600` 3-way |
| `osd_elements.c` `osdElementReadyMode()` | 기본 Rescue 모드만 표시 | `USE_FLIGHT_PLAN` → `AUTO-N/M` 표시 |

---

## 4. 미구현 또는 차이점

| 항목 | 가이드 내용 | 실제 코드 | 비고 |
|---|---|---|---|
| Waypoint 타입 8종 | 8가지 전체 지원 명시 | 8종 enum 정의됨 | `TAKEOFF`, `ALT_CHANGE`, `DELAY`, `YAW_RATE`는 현재 Rescue 로직에서 완전히 활용되지는 않음 (FLYOVER 위주 동작) |
| `FLYBY` 선회 반경 | 선회 반경 설정 가능 | `WP_TYPE_FLYBY` enum 존재하나 구체적 반경 로직은 미확인 | 기본 구조는 갖춤 |
| `LAND` 착륙 | 해당 좌표로 착륙 | `WP_TYPE_LAND` enum 존재, Rescue LANDING 단계와 연동 필요 | 기본 구조는 갖춤 |
| `HOLD` 선회 | 패턴 선회 | `WP_TYPE_HOLD` + `WP_PATTERN_ORBIT`/`FIGURE8` | 패턴 enum만 정의, 상세 구현 필요 |
| Configurator FlightPlan 탭 | 자동 표시 | 펌웨어 측에서 처리, Configurator JS 코드는 별도 | 펌웨어 MSP/MSP2 프로토콜 구현 필요 |

---

## 5. 결론

**사용자 가이드에 명시된 모든 핵심 기능이 코드로 구현되어 있습니다.**  
특히 Waypoint Mission 시스템 (`flight/mission.c`, `pg/mission.c`), 3-Way AUX 모드 전환로직, OSD Ready Mode 표시, Shuttle 모드, Pre-arm Wiggle 기능이 모두 정상 구현되었습니다.

원본 Betaflight 대비 다음과 같은 독자적 기능이 추가되었습니다:
- **Waypoint 기반 Autopilot Mission 시스템** (신규 모듈)
- **3-Way AUX 스위치 분기** (셔틀 / Autopilot / Rescue)
- **OSD `AUTO-N/M` 미션 진행 표시**
- **셔틀(Shuttle) A-B 왕복 모드** (무한/제한)
- **CPA(Closest Point of Approach) 기반 Waypoint 도착 판정**
- **Pre-arm Wiggle (ready_to_arm_wiggle_hz)**

> ⚠️ Configurator 측 FlightPlan 탭 UI는 펌웨어가 아닌 별도 저장소(`betaflight-configurator`)의 JavaScript 코드로 구현되므로, 본 보고서 범위에서 제외되었습니다.
