# Betaflight GPS Rescue & Mission Mode 코드 검증 보고서

> **검증 대상**: `saydals/autopilot` 저장소의 Betaflight 고정익 GPS Rescue 및 Mission 시스템  
> **검증 파일**: `mission.h`, `mission.c`, `gps_rescue.h`, `gps_rescue.c`  
> **검증 방법**: 정적 코드 분석 (Static Code Analysis)  
> **검증 일자**: 2026-07-21  
> **검증자**: AI 코드 분석 엔진

---

## 목차

1. [시스템 개요](#1-시스템-개요)
2. [심각도별 버그 목록](#2-심각도별-버그-목록)
3. [상세 버그 분석](#3-상세-버그-분석)
4. [모드 전환 매트릭스 및 위험 경로](#4-모드-전환-매트릭스-및-위험-경로)
5. [안전 임계값 검토](#5-안전-임계값-검토)
6. [권장 수정 사항](#6-권장-수정-사항)
7. [종합 평가](#7-종합-평가)

---

## 1. 시스템 개요

### 1.1 구조

| 구성 요소          | 파일             | 역할                                                    |
| ------------------ | ---------------- | ------------------------------------------------------- |
| Mission Controller | `mission.c/h`    | 웨이포인트 목록 관리, CPA 기반 WP 전환, 글라이드 슬로프 |
| GPS Rescue Engine  | `gps_rescue.c/h` | 상태 머신, 셔틀 랜딩, PID 제어, 안전 진단               |

### 1.2 지원 모드

| 모드            | Phase                                            | 설명             |
| --------------- | ------------------------------------------------ | ---------------- |
| 일반 레스큐     | `RESCUE_FLY_HOME` → `DESCENT` → `LANDING`        | 홈 귀환 후 착륙  |
| 셔틀 랜딩       | `RESCUE_SHUTTLE` → `SHUTTLE_DESCENT` → `DESCENT` | A-B 왕복 후 하강 |
| 무한 셔틀       | `RESCUE_SHUTTLE_INFINITE`                        | A-B 무한 왕복    |
| 웨이포인트 미션 | `RESCUE_MISSION_FLY_WP`                          | WP 순차 비행     |

### 1.3 AUX 스위치 매핑

| AUX 값        | 동작                        |
| ------------- | --------------------------- |
| `< 1400`      | 무한 셔틀 모드              |
| `1400 ~ 1600` | 웨이포인트 미션 (Autopilot) |
| `> 1600`      | 일반 GPS 레스큐             |

---

## 2. 심각도별 버그 목록

### 🔴 Critical (즉시 수정 필요)

| ID     | 제목                                                                          | 위치                     | 위험도   |
| ------ | ----------------------------------------------------------------------------- | ------------------------ | -------- |
| CRIT-1 | 미션 블록에서 `phase` 강제 덮어쓰기로 안전 전환 무효화                        | `gps_rescue.c:1470`      | **HIGH** |
| CRIT-2 | `RESCUE_SHUTTLE_INFINITE` → `INITIALIZE` 전환 시 상태 초기화 누락             | `gps_rescue.c:1550`      | **HIGH** |
| CRIT-3 | 랜딩 타이머 변수 (`attainAltStartTime`) 재사용으로 타이머 오작동              | `gps_rescue.c:650`       | **HIGH** |
| CRIT-4 | 셔틀 완료 시 `descentAltReached` 미체크로 무한 셔틀 하강                      | `gps_rescue.c:1530`      | **HIGH** |
| CRIT-5 | `SHUTTLE_DESCENT`/`DESCENT` → `SHUTTLE_INFINITE` 전환 시 셔틀 포인트 미초기화 | `gps_rescue.c:1545,1560` | **HIGH** |
| CRIT-6 | 웨이포인트 전환 시 `wasClosing` 초기화 누락으로 즉시 스킵                     | `mission.c:190`          | **HIGH** |

### 🟠 High (중기 수정 권장)

| ID     | 제목                                                                | 위치                | 위험도 |
| ------ | ------------------------------------------------------------------- | ------------------- | ------ |
| HIGH-1 | `handleShuttleProgress()`에서 직접 phase 변경 시 `lastPhase` 불일치 | `gps_rescue.c:430`  | MEDIUM |
| HIGH-2 | `RESCUE_FLY_HOME`에서 미션 진입 허용으로 상태 충돌                  | `mission.c:75`      | MEDIUM |
| HIGH-3 | `aPointValid` 설정 시 `takeoffVectorCaptured` 미동기화              | `gps_rescue.c:1480` | MEDIUM |
| HIGH-4 | 급하강 종료 후 `targetAltitudeCm` 미재설정                          | `gps_rescue.c:550`  | MEDIUM |
| HIGH-5 | 미션 모드 안전 진단 (`secondsFailing`) 누락                         | `gps_rescue.c:750`  | MEDIUM |
| HIGH-6 | 셔틀 모드에서 Yaw PI 제어 활성화 조건 과도 제한                     | `gps_rescue.c:480`  | MEDIUM |
| HIGH-7 | `initialVelocityLow` 지역 변수로 인한 속도 제어 불안정              | `gps_rescue.c:1420` | MEDIUM |

### 🟡 Medium (개선 권장)

| ID    | 제목                                                   | 위치                | 위험도 |
| ----- | ------------------------------------------------------ | ------------------- | ------ |
| MED-1 | `prevAltMInitialized` 파일 스코프로 인한 한 루프 지연  | `gps_rescue.c:180`  | LOW    |
| MED-2 | `missionStop()` 호출 후 `return true`로 이중 전환      | `mission.c:200`     | LOW    |
| MED-3 | `RESCUE_LANDING`에서 충격 감지 한 루프 지연            | `gps_rescue.c:1580` | LOW    |
| MED-4 | 무한 셔틀에서 `currentShuttleTrips` 미증가             | `gps_rescue.c:440`  | LOW    |
| MED-5 | `missionIsActive()`와 `phase` 불일치 시 OSD 표시 오류  | `gps_rescue.c:1620` | LOW    |
| MED-6 | `groundSpeedCmS`가 0일 때 속도 제어 비정상             | `gps_rescue.c:1470` | LOW    |
| MED-7 | `throttleHover > throttleMax` 설정 시 쓰로틀 제한 오류 | `gps_rescue.c:220`  | LOW    |

---

## 3. 상세 버그 분석

### 🔴 CRIT-1: 미션 블록에서 `phase` 강제 덮어쓰기

**위치**: `gps_rescue.c` ~라인 1470

```c
#ifdef USE_FLIGHT_PLAN
if (missionIsActive()) {
    // ... AUX 체크 및 missionStop() ...
    missionUpdateTargetOnly();
    if (!missionIsActive()) {
        newGPSData = false;
        return;
    }
    rescueState.phase = RESCUE_MISSION_FLY_WP;  // ← 🔴 강제 설정
    performSanityChecks();
    rescueAttainPosition();
    missionCheckAdvance();
    newGPSData = false;
    return;
}
#endif
```

**문제 분석**:

1. `missionUpdateTargetOnly()`는 낶부에서 `RESCUE_DESCENT`, `RESCUE_LANDING`, `RESCUE_ABORT` 등을 감지하면 `missionStop()`을 호출합니다.
2. `missionStop()`은 `rescueState.phase`를 `RESCUE_FLY_HOME` 또는 `RESCUE_SHUTTLE_INFINITE`로 **안전하게 전환**합니다.
3. 하지만 `gpsRescueUpdate()`의 미션 블록은 `missionUpdateTargetOnly()` 리턴 후 **무조건** `rescueState.phase = RESCUE_MISSION_FLY_WP`로 덮어씁니다.
4. 이로 인해 `missionStop()`이 수행한 안전 전환이 **완전히 무효화**됩니다.

**재현 시나리오**:

```
[루프 N]   RESCUE_MISSION_FLY_WP 실행 중
           → missionUpdateTargetOnly() 낶에서 RESCUE_LANDING 감지
           → missionStop() 호출 → phase = RESCUE_FLY_HOME
           → 그러나 다음 라인: phase = RESCUE_MISSION_FLY_WP (덮어씀!)
           → 결과: RESCUE_LANDING 상태에서도 미션 계속 실행
```

**영향**:  

- 착륙 중에도 미션이 계속 실행되어 웨이포인트 타겟으로 비행  
- 고도가 낮은 상태에서 급격한 방향 전환으로 **기체 손상 가능**

**수정 제안**:

```c
#ifdef USE_FLIGHT_PLAN
if (missionIsActive()) {
    const uint16_t auxVal = getRescueAuxValue();
    if (failsafeIsReceivingRxData() && (auxVal < 1400 || auxVal >= 1600)) {
        missionStop();
        newGPSData = false;
        return;
    }
    missionUpdateTargetOnly();
    if (!missionIsActive()) {
        newGPSData = false;
        return;
    }
    // 🔴 삭제: rescueState.phase = RESCUE_MISSION_FLY_WP;
    // missionStart()에서 이미 설정되었거나, 현재 phase를 존중
    if (rescueState.phase != RESCUE_MISSION_FLY_WP) {
        // missionUpdateTargetOnly()가 phase를 변경했다면 존중
        newGPSData = false;
        return;
    }
    performSanityChecks();
    rescueAttainPosition();
    missionCheckAdvance();
    newGPSData = false;
    return;
}
#endif
```

---

### 🔴 CRIT-2: `SHUTTLE_INFINITE` → `INITIALIZE` 전환 시 초기화 누락

**위치**: `gps_rescue.c` ~라인 1550

```c
case RESCUE_SHUTTLE_INFINITE:
    if (failsafeIsReceivingRxData() && getRescueAuxValue() >= 1400) 
        rescueState.phase = RESCUE_INITIALIZE;
    break;
```

**문제 분석**:

1. AUX >= 1400이면 `RESCUE_INITIALIZE`로 즉시 전환됩니다.
2. `gpsRescueUpdate()`의 `lastPhase` 체크는 **다음 루프**에서야 반응합니다.
3. 현재 루프에서는 `rescueAttainPosition()`이 `RESCUE_SHUTTLE_INFINITE` 핸들러를 호출합니다.
4. 다음 루프에서 `lastPhase = RESCUE_SHUTTLE_INFINITE`, `rescueState.phase = RESCUE_INITIALIZE`로 감지되어 초기화 블록이 실행됩니다.
5. **하지만** `RESCUE_INITIALIZE` 핸들러는 이미 현재 루프에서 실행되지 않습니다 — `switch` 문의 `RESCUE_SHUTTLE_INFINITE` 케이스가 실행되었기 때문입니다.
6. `RESCUE_INITIALIZE` 핸들러는 다음 루프에서야 실행되며, 이때 `lastPhase`는 `RESCUE_INITIALIZE`로 업데이트됩니다.
7. 결과적으로 `RESCUE_INITIALIZE`로 전환된 **첫 루프**에서:
   - `shuttleInfinite`가 `true`인 채로 `RESCUE_INITIALIZE` 핸들러가 실행
   - `cpaDistToTargetCm`, `cpaWasClosing` 등이 초기화되지 않음
   - `turnDirectionSign`이 이전 셔틀의 값을 유지

**재현 시나리오**:

```
[루프 N]   RESCUE_SHUTTLE_INFINITE 실행
           AUX >= 1400 감지
           → phase = RESCUE_INITIALIZE
           → rescueAttainPosition()은 SHUTTLE_INFINITE 핸들러 호출
           → lastPhase = RESCUE_SHUTTLE_INFINITE (업데이트 안 됨)

[루프 N+1] lastPhase(=SHUTTLE_INFINITE) != phase(=INITIALIZE)
           → 초기화 블록 실행 (shuttleInfinite=false 등)
           → rescueAttainPosition()은 INITIALIZE 핸들러 호출
           → lastPhase = RESCUE_INITIALIZE
```

**영향**:  

- 한 루프 동안 셔틀 상태 변수가 초기화되지 않은 채로 `RESCUE_INITIALIZE` 실행  
- `shuttleInfinite = true`인 상태에서 `RESCUE_INITIALIZE`가 `aPointValid` 체크 등을 수행  
- 예상치 못한 분기로 인해 **DO_NOTHING** 또는 잘못된 phase로 전환 가능

**수정 제안**:

```c
case RESCUE_SHUTTLE_INFINITE:
    if (failsafeIsReceivingRxData() && getRescueAuxValue() >= 1400) {
        gpsRescueResetState();  // ← 즉시 초기화
        rescueState.phase = RESCUE_INITIALIZE;
    }
    break;
```

---

### 🔴 CRIT-3: 랜딩 타이머 변수 재사용

**위치**: `gps_rescue.c` ~라인 650

```c
static void handleLandingPhase(void)
{
    // ...
    if (attainAltStartTime == 0) attainAltStartTime = micros();
    if (cmpTimeUs(micros(), attainAltStartTime) >= ATTAIN_ALT_TIMEOUT_US) {
        rescueThrottle = PWM_RANGE_MIN;  // 3초 후 모터 정지
    } else {
        rescueThrottle = (float)gpsRescueConfig()->throttleMin;
    }
}
```

**문제 분석**:

1. `attainAltStartTime`은 `RESCUE_ATTAIN_ALT`에서 상승 타임아웃 측정용으로 사용됩니다.
2. `RESCUE_ATTAIN_ALT` → `RESCUE_FLY_HOME` 전환 시 `attainAltStartTime = 0`으로 초기화됩니다.
3. 하지만 **다른 경로**에서는 초기화되지 않습니다:
   - `missionStop()` → `RESCUE_FLY_HOME` → `RESCUE_DESCENT` → `RESCUE_LANDING`
   - `RESCUE_SHUTTLE_DESCENT` → `RESCUE_DESCENT` → `RESCUE_LANDING`
   - `RESCUE_SHUTTLE_INFINITE` → `RESCUE_INITIALIZE` → ... → `RESCUE_LANDING`
4. `gpsRescueResetState()`에는 `attainAltStartTime = 0`이 포함되어 있지만, `missionStop()`에서는 `gpsRescueResetState()`를 호출하지만 `RESCUE_FLY_HOME`으로 직접 전환합니다.
5. `RESCUE_FLY_HOME` → `RESCUE_DESCENT` → `RESCUE_LANDING` 경로에서는 `attainAltStartTime`이 초기화되지 않습니다.

**재현 시나리오**:

```
[미션 완료] missionStop() 호출 → phase = RESCUE_FLY_HOME
            gpsRescueResetState() 호출 → attainAltStartTime = 0 (정상)

[그러나]    SHUTTLE_DESCENT → DESCENT → LANDING 경로:
            gpsRescueResetState() 미호출
            attainAltStartTime이 이전 값 유지 (예: 0이 아님)
            → 랜딩 진입 즉시 3초 타이머가 이미 만료된 것으로 판단
            → 즉시 PWM_RANGE_MIN (모터 정지)
```

**영향**:  

- 랜딩 직후 즉시 모터 정지 또는 반대로 타이머가 작동하지 않음  
- 고도가 충분히 낮지 않은 상태에서 모터 정지 시 **추락**

**수정 제안**:

```c
// 별도의 랜딩 타이머 변수 사용
static timeUs_t landingStartTime = 0;

static void handleLandingPhase(void)
{
    // ...
    if (landingStartTime == 0) landingStartTime = micros();
    if (cmpTimeUs(micros(), landingStartTime) >= LANDING_TIMEOUT_US) {
        rescueThrottle = PWM_RANGE_MIN;
    } else {
        rescueThrottle = (float)gpsRescueConfig()->throttleMin;
    }
}

// Phase 전환 시 초기화
if (rescueState.phase == RESCUE_LANDING && lastPhase != RESCUE_LANDING) {
    landingStartTime = 0;
}
```

---

### 🔴 CRIT-5: 셔틀 포인트 미초기화

**위치**: `gps_rescue.c` ~라인 1545, 1560

```c
case RESCUE_SHUTTLE_DESCENT:
    if (failsafeIsReceivingRxData() && getRescueAuxValue() < 1400) {
        shuttleInfinite = true; 
        rescueState.phase = RESCUE_SHUTTLE_INFINITE;  // ← initShuttlePoints() 미호출
        break;
    }

// ...

case RESCUE_DESCENT:
    if (failsafeIsReceivingRxData() && getRescueAuxValue() < 1400) {
        shuttleInfinite = true; 
        rescueState.phase = RESCUE_SHUTTLE_INFINITE;  // ← initShuttlePoints() 미호출
        break;
    }
```

**문제 분석**:

1. `RESCUE_SHUTTLE_DESCENT` 또는 `RESCUE_DESCENT`에서 AUX < 1400이면 `RESCUE_SHUTTLE_INFINITE`로 전환됩니다.
2. `shuttleInfinite = true`만 설정하고 `initShuttlePoints()`를 호출하지 않습니다.
3. `initShuttlePoints()`는 `shuttlePointA`, `shuttlePointB`, `abVecLat`, `abVecLon` 등을 초기화합니다.
4. 이전 셔틀의 좌표가 그대로 유지되어 예상치 못한 위치에서 무한 셔틀이 시작됩니다.

**재현 시나리오**:

```
[SHUTTLE_DESCENT] shuttlePointA = rescuePointA (홈 근처)
                  shuttlePointB = 홈 방향으로 30m

[AUX < 1400]    → phase = SHUTTLE_INFINITE
                  → initShuttlePoints() 미호출
                  → shuttlePointA/B가 이전 값 유지
                  → 홈 근처에서 셔틀 시작 (의도와 다름)
```

**영향**:  

- 무한 셔틀이 예상치 못한 위치(홈 근처 등)에서 시작  
- **충돌 위험**

**수정 제안**:

```c
case RESCUE_SHUTTLE_DESCENT:
    if (failsafeIsReceivingRxData() && getRescueAuxValue() < 1400) {
        shuttleInfinite = true;
        initShuttlePoints();  // ← 추가
        rescueState.phase = RESCUE_SHUTTLE_INFINITE;
        break;
    }
```

---

### 🔴 CRIT-6: 웨이포인트 즉시 스킵

**위치**: `mission.c` ~라인 190

```c
bool missionCheckAdvance(void)
{
    // ...
    if (dCm < GPS_RESCUE_TOUCH_ACTIVATION_CM && dCm >= 0) {
        if (prevDistCm < 0) {
            prevDistCm = dCm;
            return false;  // ← wasClosing 초기화 없음
        }
        bool isClosing = (dCm < prevDistCm - 20.0f);
        bool touchCPA = false;
        if (!isClosing && wasClosing) {
            touchCPA = true;
        }
        wasClosing = isClosing;
        prevDistCm = dCm;
        // ...
    }
}
```

**문제 분석**:

1. 새 웨이포인트 진입 시 `prevDistCm < 0`이면 `prevDistCm = dCm`만 설정하고 리턴합니다.
2. `wasClosing`은 초기화되지 않습니다 — 이전 웨이포인트의 값을 유지합니다.
3. 만약 이전 웨이포인트에서 `wasClosing = true`였다면:
   - 새 웨이포인트 첫 프레임: `prevDistCm = dCm`, `wasClosing = true` (유지)
   - 두 번째 프레임: `dCm`이 증가하면 `isClosing = false`
   - `!isClosing && wasClosing` → `touchCPA = true`
   - 즉시 웨이포인트 전환

**재현 시나리오**:

```
[WP 0]      wasClosing = true (CPA 통과 직전)
            touchCPA = true → WP 1로 전환

[WP 1 진입] prevDistCm = -1.0f → 초기화
            첫 프레임: prevDistCm = dCm (예: 1500cm)
                      wasClosing = true (이전 값 유지!)
            두 번째 프레임: dCm = 1520cm (멀어짐)
                           isClosing = false
                           touchCPA = true (즉시!)
                           → WP 2로 전환
```

**영향**:  

- 웨이포인트에 진입하자마자 즉시 다음 WP로 스킵  
- 미션 경로를 거의 따라가지 않음  
- **충돌 또는 길 손실**

**수정 제안**:

```c
if (dCm < GPS_RESCUE_TOUCH_ACTIVATION_CM && dCm >= 0) {
    if (prevDistCm < 0) {
        prevDistCm = dCm;
        wasClosing = false;  // ← 초기화 추가
        return false;
    }
    // ...
}
```

---

## 4. 모드 전환 매트릭스 및 위험 경로

### 4.1 정상 전환 경로

```
┌─────────────┐     AUX>1600      ┌─────────────┐
│    IDLE     │ ─────────────────→│  INITIALIZE │
└─────────────┘                   └──────┬──────┘
                                         │
           ┌─────────────────────────────┼─────────────────────────────┐
           │                             │                             │
           ▼                             ▼                             ▼
    ┌─────────────┐              ┌─────────────┐              ┌─────────────┐
    │  ATTAIN_ALT │              │ FLY_HOME    │              │SHUTTLE_INF  │◄────┐
    └──────┬──────┘              └──────┬──────┘              └──────┬──────┘     │
           │                            │                            │          │
           │                            ▼                            │          │
           │                     ┌─────────────┐                     │          │
           │                     │   SHUTTLE   │                     │          │
           │                     └──────┬──────┘                     │          │
           │                            │                            │          │
           │                            ▼                            │          │
           │                     ┌─────────────┐                     │          │
           │                     │SHUTTLE_DESC │                     │          │
           │                     └──────┬──────┘                     │          │
           │                            │                            │          │
           │                            ▼                            │          │
           │                     ┌─────────────┐                     │          │
           └────────────────────→│   DESCENT   │◄────────────────────┘          │
                                 └──────┬──────┘                                │
                                        │                                       │
                                        ▼                                       │
                                 ┌─────────────┐                                │
                                 │   LANDING   │                                │
                                 └──────┬──────┘                                │
                                        │                                       │
                                        ▼                                       │
                                 ┌─────────────┐                                │
                                 │  COMPLETE   │                                │
                                 └─────────────┘                                │
                                                                                │
    ┌─────────────┐              AUX<1400                                       │
    │   MISSION   │◄────────────────────────────────────────────────────────────┘
    │  FLY_WP     │
    └─────────────┘
```

### 4.2 위험 전환 경로 (버그로 인해 가능)

| #   | 출발               | 도착                          | 원인                              | 위험도 |
| --- | ------------------ | ----------------------------- | --------------------------------- | ------ |
| 1   | `MISSION_FLY_WP`   | `MISSION_FLY_WP` (강제)       | CRIT-1: phase 덮어쓰기            | 🔴     |
| 2   | `SHUTTLE_INFINITE` | `INITIALIZE` (미초기화)       | CRIT-2: 상태 초기화 누락          | 🔴     |
| 3   | `SHUTTLE_DESCENT`  | `SHUTTLE_INFINITE` (미초기화) | CRIT-5: 셔틀 포인트 미초기화      | 🔴     |
| 4   | `DESCENT`          | `SHUTTLE_INFINITE` (미초기화) | CRIT-5: 셔틀 포인트 미초기화      | 🔴     |
|     |                    |                               |                                   |        |
| 6   | `FLY_HOME`         | `MISSION_FLY_WP` (충돌)       | HIGH-2: 미션 진입 허용            | 🟠      |
| 7   | `MISSION_FLY_WP`   | `FLY_HOME` (타이머 오류)      | CRIT-3: attainAltStartTime 재사용 | 🔴     |

---

## 

## 6. 권장 수정 사항

### 6.1 즉시 수정 (HIGH 우선순위)

#### 수정 1: 미션 블록 phase 덮어쓰기 제거

**파일**: `gps_rescue.c`
**위치**: `gpsRescueUpdate()` 함수 내 미션 블록

```c
#ifdef USE_FLIGHT_PLAN
if (missionIsActive()) {
    const uint16_t auxVal = getRescueAuxValue();
    if (failsafeIsReceivingRxData() && (auxVal < 1400 || auxVal >= 1600)) {
        missionStop();
        newGPSData = false;
        return;
    }
    missionUpdateTargetOnly();
    if (!missionIsActive()) {
        newGPSData = false;
        return;
    }

    // 🔴 기존: rescueState.phase = RESCUE_MISSION_FLY_WP;
    // ✅ 수정: missionUpdateTargetOnly()가 phase를 변경했는지 확인
    if (rescueState.phase != RESCUE_MISSION_FLY_WP && 
        rescueState.phase != RESCUE_FLY_HOME &&
        rescueState.phase != RESCUE_SHUTTLE_INFINITE) {
        // missionUpdateTargetOnly()가 안전 전환을 수행함
        newGPSData = false;
        return;
    }

    // 안전한 phase만 강제 설정
    if (rescueState.phase != RESCUE_MISSION_FLY_WP) {
        rescueState.phase = RESCUE_MISSION_FLY_WP;
    }

    performSanityChecks();
    rescueAttainPosition();
    missionCheckAdvance();
    newGPSData = false;
    return;
}
#endif
```

#### 수정 2: `SHUTTLE_INFINITE` → `INITIALIZE` 전환 시 초기화

**파일**: `gps_rescue.c`
**위치**: `gpsRescueUpdate()` 함수 내 `RESCUE_SHUTTLE_INFINITE` 케이스

```c
case RESCUE_SHUTTLE_INFINITE:
    if (failsafeIsReceivingRxData() && getRescueAuxValue() >= 1400) {
        gpsRescueResetState();  // ✅ 즉시 초기화 추가
        rescueState.phase = RESCUE_INITIALIZE;
    }
    break;
```

#### 수정 3: 랜딩 타이머 분리

**파일**: `gps_rescue.c`

```c
// 전역 변수 추가
static timeUs_t landingStartTime = 0;

// handleLandingPhase 수정
static void handleLandingPhase(void)
{
    currentVCLat = GPS_home[0];
    currentVCLon = GPS_home[1];
    GPS_distance_cm_bearing(&gpsSol.llh.lat, &gpsSol.llh.lon, &currentVCLat, &currentVCLon, 
                            &rescueState.intent.distanceToTargetCm, &rescueState.intent.directionToTargetCd);
    gpsRescueAngle[AI_PITCH] = landingPitch * 100.0f;
    gpsRescueAngle[AI_ROLL]  = 0.0f;
    rescueYaw = 0.0f;

    if (landingStartTime == 0) landingStartTime = micros();
    if (cmpTimeUs(micros(), landingStartTime) >= LANDING_TIMEOUT_US) {
        rescueThrottle = PWM_RANGE_MIN;
    } else {
        rescueThrottle = (float)gpsRescueConfig()->throttleMin;
    }
}

// phase 전환 시 초기화 (gpsRescueUpdate 내)
if (rescueState.phase == RESCUE_LANDING && lastPhase != RESCUE_LANDING) {
    landingStartTime = 0;
}
```

#### 

#### 수정 5: 셔틀 포인트 초기화

**파일**: `gps_rescue.c`
**위치**: `gpsRescueUpdate()` 함수 내 `RESCUE_SHUTTLE_DESCENT` 및 `RESCUE_DESCENT` 케이스

```c
case RESCUE_SHUTTLE_DESCENT:
    if (failsafeIsReceivingRxData() && getRescueAuxValue() < 1400) {
        shuttleInfinite = true;
        initShuttlePoints();  // ✅ 추가
        rescueState.phase = RESCUE_SHUTTLE_INFINITE;
        break;
    }
    // ...

case RESCUE_DESCENT:
    if (failsafeIsReceivingRxData() && getRescueAuxValue() < 1400) {
        shuttleInfinite = true;
        initShuttlePoints();  // ✅ 추가
        rescueState.phase = RESCUE_SHUTTLE_INFINITE;
        break;
    }
    // ...
```

#### 수정 6: 웨이포인트 `wasClosing` 초기화

**파일**: `mission.c`
**위치**: `missionCheckAdvance()` 함수

```c
if (dCm < GPS_RESCUE_TOUCH_ACTIVATION_CM && dCm >= 0) {
    if (prevDistCm < 0) {
        prevDistCm = dCm;
        wasClosing = false;  // ✅ 초기화 추가
        return false;
    }
    // ...
}
```

### 6.2 중기 수정 (MEDIUM 우선순위)

| #   | 수정 내용                                               | 파일           | 위치                      |
| --- | ------------------------------------------------------- | -------------- | ------------------------- |
| 1   | `handleShuttleProgress()`에서 phase 변경 시 플래그 사용 | `gps_rescue.c` | `handleShuttleProgress()` |
| 2   | `missionStart()`에서 `RESCUE_FLY_HOME` 차단             | `mission.c`    | `missionStart()`          |
| 3   | `aPointValid` 설정 시 `takeoffVectorCaptured` 동기화    | `gps_rescue.c` | `RESCUE_FLY_HOME` 케이스  |
| 4   | 급하강 종료 후 `targetAltitudeCm` 재설정                | `gps_rescue.c` | `handleDescentPhase()`    |
| 5   | 미션 모드 안전 진단 추가                                | `gps_rescue.c` | `performSanityChecks()`   |
| 6   | 셔틀 Yaw PI 활성화 조건 완화                            | `gps_rescue.c` | `handleShuttleProgress()` |
| 7   | `initialVelocityLow` static 변수화                      | `gps_rescue.c` | `gpsRescueUpdate()`       |

---

## 약점 ⚠️

| 항목                     | 설명                               | 심각도 |
| ------------------------ | ---------------------------------- | ------ |
| **Phase 덮어쓰기 버그**  | 미션 블록에서 안전 전환을 무효화   | 🔴     |
| **상태 초기화 누락**     | 모드 전환 시 `lastPhase` 체크 지연 | 🔴     |
| **타이머 변수 재사용**   | 랜딩 타이머와 상승 타이머 공유     | 🔴     |
| **셔틀 포인트 미초기화** | 모드 전환 시 좌표 초기화 누락      | 🔴     |
| **웨이포인트 즉시 스킵** | `wasClosing` 초기화 누락           | 🔴     |
| **상태 머신 일관성**     | 일부 엣지 케이스에서 상태 불일치   | 🟠      |



### 실제로 잡아야 할 수정내용.

---

## 1. 실제 버그를 수정한 것으로 판단되는 항목 ( 위와 중복일 경우 패스)

### 1-1. `mission.c` — `missionRemove()`에서 currentMissionWpIndex 미보정 (★가장 중요한 버그)

- **원본**: 웨이포인트 삭제 시 배열은 한 칸씩 당겨지지만 `currentMissionWpIndex`는 그대로 둠.
- **수정**: 삭제 위치보다 뒤에 있던 인덱스면 `currentMissionWpIndex--`, 마지막 WP 삭제 후 범위를 벗어나면 0으로 보정. 삭제된 뒤쪽 PG 슬롯도 memset으로 정리.
- **판정**: 실제 버그. 현재 비행 중인 WP보다 앞선 WP를 삭제하면 배열이 당겨지면서 `currentMissionWpIndex`가 엉뚱한(한 칸 밀린) 웨이포인트를 가리키게 됩니다. 비행 중 미션 편집 시 잘못된 좌표로 비행할 수 있는 심각한 버그였고, 수정이 타당합니다.

### 1-2. `mission.c` — `missionCheckAdvance()`의 `wasClosing` 미초기화 (CRIT-6)

- **원본**: 타임아웃 강제 스킵, CPA 첫 진입, WP 전환, 활성화 범위 이탈 시 `prevDistCm`만 리셋하고 `wasClosing`은 리셋하지 않음.
- **수정**: 위 4곳 모두에서 `wasClosing = false`를 추가.
- **판정**: 실제 버그. 이전 웨이포인트에서 `wasClosing = true`로 남은 상태가 다음 웨이포인트의 CPA 판정에 그대로 이어지면, 새 WP 진입 직후 "거리가 줄다가 늘어나는 것"을 아직 확인하지 않았는데도 오탐(false positive)으로 조기에 WP를 통과 처리할 수 있습니다. 수정이 맞습니다.

### 1-3. `mission.c` — `missionStop()`에서 `wpGlideInitialized` 미초기화

- **원본**: 미션 종료 시 글라이드 슬로프 보간 상태(`wpGlideInitialized`)를 리셋하지 않음.
- **수정**: `wpGlideInitialized = false;` 추가.
- **판정**: 실제 버그(경미). 다음 미션 재시작 시 이전 글라이드 시작 고도/거리 값이 잔존해 첫 웨이포인트에서 잘못된 고도 보간이 한 번 일어날 수 있음. 초기화 누락이 맞고, 수정이 타당합니다.

### 1-4. `mission.c` — `missionClear()`에서 인덱스/활성 상태 미초기화

- **원본**: 웨이포인트 배열과 개수만 초기화, `currentMissionWpIndex`/`isMissionActive`는 그대로.
- **수정**: 둘 다 초기화 추가.
- **판정**: 실제 방어적 수정. `missionWpCount = 0`이면 `missionIsActive()`가 항상 false를 반환하므로 즉각적인 오작동 가능성은 낮지만, 잔존 인덱스 값이 이후 로직 가정을 깨뜨릴 수 있어 정당한 방어 코드입니다.

### 1-5. `mission.c` — `missionStart()`에서 RESCUE_FLY_HOME / RESCUE_ATTAIN_ALT 중 미션 시작 차단 (CRIT-7)

- **원본**: DESCENT/LANDING/SHUTTLE_DESCENT/ABORT/DO_NOTHING/COMPLETE에서만 미션 시작을 막음.
- **수정**: FLY_HOME, ATTAIN_ALT 상태에서도 차단.
- **판정**: 실제 버그 가능성 높음. FLY_HOME 단계는 `aPointValid`/`takeoffVectorCaptured`라는 gps_rescue.c 쪽 상태를 함께 사용하는데, 이 상태 진행 중에 미션이 끼어들면 두 상태 머신이 동시에 좌표를 건드리는 충돌이 발생할 수 있습니다. 차단 추가는 합리적입니다.

### 1-6. `gps_rescue.c` — 착륙(LANDING) 타이머가 상승(ATTAIN_ALT) 타이머와 변수 공유 (CRIT-3)

- **원본**: `handleLandingPhase()`가 착륙 후 3초 대기 타이머로 `attainAltStartTime`을 재사용.
- **수정**: `landingStartTime`이라는 별도 변수 신설, 관련 리셋 지점(`gpsRescueResetState()`, phase 전환 블록, RESCUE_INITIALIZE 재초기화 블록)에도 반영.
- **판정**: 실제 버그. 같은 변수를 두 단계(ATTAIN_ALT 상승 타임아웃, LANDING 착지 대기)가 공유하면, 이전 단계에서 남은 타임스탬프 값에 따라 착륙 직후 곧바로(대기 없이) 스로틀을 최소로 내리거나, 반대로 의도보다 오래 대기하는 등 타이밍 오작동이 가능합니다. 변수를 분리한 것은 타당한 수정입니다.

### 1-7. `gps_rescue.c` — RESCUE_MISSION_FLY_WP 안전 진단 누락 (HIGH-5)

- **원본**: `performSanityChecks()`의 귀환 속도 기반 실패 감지(flyaway 감지)가 `RESCUE_FLY_HOME`에서만 동작.
- **수정**: `RESCUE_MISSION_FLY_WP`도 같은 검사 대상에 포함.
- **판정**: 실제 안전 공백 수정. 미션 웨이포인트 비행 중에는 GPS 귀환 실패(flyaway) 감지가 전혀 동작하지 않고 있었다는 뜻이라, 이 추가는 실질적인 안전 버그 수정으로 보입니다.

### 1-8. `gps_rescue.c` — SHUTTLE_INFINITE → INITIALIZE 전환 시 상태 미초기화 (CRIT-2)

- **원본**: 전환 시 phase만 바꾸고 `gpsRescueResetState()` 호출 없음.
- **수정**: 전환 직전에 `gpsRescueResetState()` 호출 추가.
- **판정**: 실제 버그. CPA/셔틀 관련 잔존 상태가 INITIALIZE 재계산에 영향을 줄 수 있어, 리셋 추가는 타당합니다.

### 1-9. `gps_rescue.c` — 급하강(isDescentFalling) 종료 후 targetAltitudeCm 미갱신 (HIGH-4)

- **원본**: 급하강이 끝나도 `targetAltitudeCm`을 재설정하지 않음(급하강 중에는 이 값이 고도 제어 루프에 쓰이지 않고 방치되어 있었음).
- **수정**: 급하강 종료 시 `targetAltitudeCm = descentAlt * 100.0f`로 명시적으로 재설정.
- **판정**: 실제 버그. 급하강 종료 직후 일반 PID 고도 유지 로직(`calculateAltitudePitch`)이 오래된(stale) 타겟 고도값을 기준으로 오차를 계산하면 피치 명령이 급변할 수 있습니다. 수정이 타당합니다.



추가로 수정해야할 오류들



실재 여부 및 제안 수정의 완성도 검증

### 🔴 C1: WP=0 시 RESCUE_INITIALIZE 무한 루프

- **실재 여부**: ✅ 확인됨. `mission.c`의 `missionStart()`가 WP=0일 때 `rescueState.phase = RESCUE_INITIALIZE`로 설정하는데, 이 함수의 호출부 중 하나가 **바로 `gps_rescue.c`의 `case RESCUE_INITIALIZE:` 내부**(1387행 부근, `else if (aux < 1600) { missionStart(); if (missionIsActive()) phase = RESCUE_MISSION_FLY_WP; }`)입니다. 여기서 phase가 이미 RESCUE_INITIALIZE인 상태에서 다시 RESCUE_INITIALIZE로 설정되고, 미션이 비활성이면 이 if-else 사슬에 아무 대응 `else`가 없어 phase가 전혀 바뀌지 않습니다. 다음 루프도 동일 분기를 타 무한 반복 — 실제 버그 맞습니다.
- **제안된 수정의 문제점**:
  - `missionStart()`는 **총 6곳**(`gps_rescue.c` 1281, 1390, 1416, 1439, 1539행 + mission.c 자체 흐름)에서 호출됩니다. 그중 **가장 처음 호출부(1281행, RESCUE_IDLE에서 진입 시)에서는 이 시점까지 `initialiseRescueValues()`가 한 번도 실행되지 않은 상태**입니다. GLM의 수정안은 이 조건에서 곧바로 `RESCUE_FLY_HOME`/`RESCUE_ATTAIN_ALT`로 phase를 바꾸는데, 이러면 `initialiseRescueValues()`(targetVelocityCmS, secondsFailing 등 초기화)와 **RESCUE_INITIALIZE가 원래 수행하던 홈 30m/5m 안전 검사(RESCUE_DO_NOTHING/RESCUE_ABORT 분기)를 완전히 건너뛰게 됩니다.** 즉, 이륙 직후 홈 근처(30m 이내)에서 이 조합(AUX 1400~1600, WP=0)으로 레스큐 모드를 켜면 안전검사 없이 바로 FLY_HOME으로 들어가는 **새로운 안전 결함**을 만들 수 있습니다.
  - `RESCUE_FLY_HOME`으로 갈 때 원본 로직은 `rescueState.intent.targetAltitudeCm = rescueState.intent.returnAltitudeCm;`도 함께 설정하는데, GLM 수정안에는 이 줄이 **빠져 있습니다.** 목표 고도가 갱신되지 않은 채 FLY_HOME에 진입할 수 있습니다.
- **판정**: 문제 진단은 정확하지만, **수정 위치(mission.c의 missionStart 내부)가 잘못되었고, 수정 코드 자체가 불완전/위험**합니다. 올바른 수정 지점은 `mission.c`가 아니라 **`gps_rescue.c`의 `case RESCUE_INITIALIZE:` 안, missionStart() 호출 직후**이며, `if (!missionIsActive())`일 때 최종 else 블록과 동일한 로직(고도 비교 + returnAltitudeCm 설정 + yawAttenuator)을 그대로 재사용하도록 고쳐야 다른 호출부에 부작용이 없습니다.

### 🔴 C2: missionStop()이 AUX 의도 무시

- **실재 여부**: ✅ 확인됨. `gps_rescue.c` 1298~1302행에서 AUX<1400(셔틀 의도) 또는 AUX≥1600(레스큐 의도) 둘 다 같은 `missionStop()`을 호출하지만, `missionStop()` 내부는 AUX를 전혀 보지 않고 GPS_FIX_HOME 유무만으로 분기합니다. 사용자가 AUX<1400으로 셔틀을 의도해도 홈픽스가 있으면 FLY_HOME으로 가버리는 것은 실제 버그입니다.
- **제안된 수정의 문제점**: `missionStop()`은 이 호출부 외에 **mission.c 내부에서도 3곳(75, 152, 216, 249행)** 호출됩니다. 이 중 152행 호출부는 **`missionUpdateTargetOnly()`가 phase를 ABORT/DO_NOTHING/LANDING/DESCENT/SHUTTLE_DESCENT로 감지했을 때** 실행되는 "안전 상황"이며, 이때는 AUX 값이 아니라 안전 상태가 우선이어야 합니다. GLM 수정안처럼 `missionStop()` 전체에 AUX 체크를 무조건 넣으면, 이 안전-트리거 호출부에서도 조종사의 (의도치 않은) AUX 값에 따라 셔틀/귀환이 뒤바뀌는 부작용이 생길 수 있습니다. 즉 **호출 컨텍스트를 구분하지 않고 일괄 수정하는 것은 위험**하며, 최소한 파라미터(`missionStop(bool respectAux)` 등)로 분리해야 안전합니다.
  - 또한 `getRescueAuxValue()`가 `gps_rescue.c`의 `static` 함수라 mission.c에서 못 부른다는 점은 GLM도 스스로 지적했지만, **실제 수정 코드 예시에는 이 문제를 반영하지 않고 그냥 호출하고 있어(6절에서만 별도로 언급) 코드 예시 자체는 컴파일되지 않는 상태**입니다.
- **판정**: 문제 진단은 정확하나, **제안 수정은 호출 컨텍스트 구분 없이 적용되어 새로운 부작용을 유발할 수 있고, 예시 코드 자체가 미완성(컴파일 불가)** 상태입니다.

### 🔴 C3: lastPhase 전환 시 shuttleInfinite 무조건 리셋

- **실재 여부**: ✅ 확인됨. `gps_rescue.c`의 "phase != lastPhase" 블록은 셔틀 단계 간 전환(`SHUTTLE_DESCENT → SHUTTLE_INFINITE` 등)에서도 무조건 `shuttleInfinite = false`로 되돌립니다. `RESCUE_SHUTTLE_DESCENT`/`RESCUE_DESCENT`의 아ux<1400 분기는 `shuttleInfinite = true`를 설정하고 `break`하는데, 그 직후(다음 루프 최상단) 이 블록이 다시 `false`로 덮어써 버릴 수 있습니다.
- **제안된 수정**: `isShuttlePhase(phase) && isShuttlePhase(lastPhase)`일 때만 리셋을 건너뛰자는 안 — 방향은 타당합니다. 다만 `initShuttlePoints()` 미호출 문제(M2/M3)와 맞물려 있어서, C3만 단독으로 고치면 `shuttleInfinite`는 유지되지만 `shuttlePointA/B` 좌표는 여전히 초기화되지 않은 채로 남을 수 있습니다. **C3와 M2/M3는 함께 고쳐야 실효성이 있습니다** (보고서에도 이 연관성 언급은 없음).
- **판정**: 진단과 방향은 타당. 다만 M2/M3와의 상호 의존성을 명시하지 않아 "이것만 고치면 끝"이라는 인상을 줄 수 있음.

### 🟠 M1: SHUTTLE 카운트 완료 체크가 AUX 체크보다 먼저 실행

- **실재 여부**: ✅ 확인됨(코드 순서 그대로).
- **다만 실질 영향은 보고서가 말하는 것보다 작습니다.** `RESCUE_SHUTTLE_DESCENT` case 자체에 이미 `aux < 1400` 시 `SHUTTLE_INFINITE`로 보내는 로직이 있어서, 이 버그의 실제 효과는 "아ux<1400으로 셔틀을 유지하고 싶어도 트립 완료 순간 딱 한 프레임(10ms) 동안 SHUTTLE_DESCENT를 스쳐 지나간 뒤 다음 프레임에 바로 SHUTTLE_INFINITE로 복귀"하는 정도입니다. **Major보다는 경미한 타이밍 이슈에 가깝습니다.**
- **제안된 수정**: 코드 순서 변경 자체는 타당하고 부작용도 없어 보입니다.

### 🟠 M2/M3: SHUTTLE_DESCENT/DESCENT → SHUTTLE_INFINITE 전환 시 initShuttlePoints() 누락

- **실재 여부**: ✅ 확인됨. FLY_HOME의 동일 전환에는 `initShuttlePoints()`가 있는데 이 두 곳엔 없습니다. 셔틀 A/B 포인트가 이전 값(혹은 미초기화 값)으로 남아 있을 위험이 실재합니다.
- **판정**: 정확한 지적, 제안 수정(두 곳 모두 `initShuttlePoints()` 추가)도 타당합니다.

### 🟠 M4: SHUTTLE_DESCENT/DESCENT에서 AUX 1400~1600, ≥1600 미지원

- **실재 여부**: 코드상 사실이지만, **이것이 "버그"인지는 논쟁의 여지가 있습니다.** 이전 검토(@ 파일)에서 확인된 CRIT-7 수정은 오히려 "FLY_HOME/ATTAIN_ALT 진행 중 미션 재진입을 막아야 안전하다"는 방향이었습니다. DESCENT/SHUTTLE_DESCENT(최종 하강 단계)에서 다시 미션이나 일반 레스큐로 전환을 허용하자는 이 제안은 **동일한 종류의 상태 충돌(좌표/aPointValid/takeoffVectorCaptured 등)을 재도입할 위험**이 있어, 오히려 의도적으로 막아둔 안전장치일 가능성이 있습니다.
- **판정**: **버그가 아니라 설계 선택일 가능성이 높은 항목을 버그로 분류**했을 수 있습니다. 최소한 기체 안전성 검증 없이 그대로 적용하면 안 됩니다.

### 🟠 M5: SHUTTLE_DESCENT → DESCENT 직접 전환 시 I-term 미리셋

- **실재 여부**: ✅ 확인됨(원본 `handleShuttleProgress()` 563~566행). 실제로 유효한 지적입니다.
- **중요 참고**: 이 코드 블록은 **1차 검토에서 다룬 @ 수정본(`_gps_rescue.c`)에서는 통째로 삭제**되어 있었고, 삭제 이유로 "상위 `gpsRescueUpdate()`에서 플래그로 처리"라는 주석만 남아 있었는데 실제 대체 로직은 찾지 못했습니다(이전 보고서 2-1 항목 참조). 즉 **같은 코드 지점을 두 AI가 서로 다르게 건드렸고, 어느 쪽도 완전한 해법을 제시하지 못한 상태**입니다. GLM의 제안(I-term 리셋 추가)이 오히려 더 안전한 방향이나, 이 경우 여전히 "이 블록 자체가 유지되어야 SHUTTLE_DESCENT→DESCENT 전환이 일어난다"는 전제가 필요합니다.
- **판정**: 유효한 지적, 수정안도 타당함.



🔴 신규: `missionStop()`이 안전 트리거로 설정된 phase를 되돌림

- **위치**: `gps_rescue.c` 1298행 부근 및 `mission.c`의 `missionUpdateTargetOnly()` (152행 `missionStop()` 호출부).
- **재현 경로**:
  1. 미션 비행 중(`RESCUE_MISSION_FLY_WP`) `performSanityChecks()`가 실패를 감지해 `rescueState.phase = RESCUE_DO_NOTHING`으로 설정합니다(이 호출은 미션 활성 블록 내부에서 매 루프 실행됨).
  2. 다음 루프, `missionIsActive()`는 여전히 true이므로 미션 블록에 다시 진입 → `missionUpdateTargetOnly()`가 `gpsRescueGetPhase() == RESCUE_DO_NOTHING`을 감지 → **`missionStop()`을 호출**합니다.
  3. `missionStop()`은 GPS_FIX_HOME이 있으면 **`rescueState.phase = RESCUE_FLY_HOME`으로 되돌립니다.**
  4. 결과적으로 안전 진단이 `RESCUE_DO_NOTHING`(비상 선회 활공)으로 전환시킨 지 한 프레임 만에, 미션 모듈이 이를 감지하고 다시 `RESCUE_FLY_HOME`(정상 비행)으로 **되돌려버려 안전 개입이 무효화**됩니다. LANDING/DESCENT/ABORT 상태에서도 동일한 문제가 발생할 수 있습니다.
- **판정**: 이것은 **Critical급 안전 문제**이며, GLM 보고서의 C2(AUX 무시)와 같은 코드 근처를 다루면서도 이 회귀 문제는 전혀 짚지 않았습니다. C2를 그대로 고쳐도(AUX 체크 추가) 이 문제는 해결되지 않습니다 — `missionUpdateTargetOnly()`가 안전-트리거 phase를 감지했을 때는 **`missionStop()`을 호출할 게 아니라 mission을 그냥 비활성화(`isMissionActive = false`)만 하고 phase는 건드리지 않아야** 합니다.





---

## 수정할 경우 실제로는 부작용이 의심되는 하지 말아야 할 수정

### 2-1. `gps_rescue.c` — `handleShuttleProgress()`에서 SHUTTLE_DESCENT→DESCENT 직접 전환 로직 삭제 (HIGH-1) ⚠️

- **원본**: A포인트 도착 시 `if (phase == RESCUE_SHUTTLE_DESCENT && descentAltReached) { phase = RESCUE_DESCENT; return; }`로 즉시 전환.
- **수정**: 이 블록을 삭제하고 주석만 "상위 gpsRescueUpdate()에서 플래그로 처리하도록 변경"이라고 남김. 그런데 실제로 어떤 플래그도 추가되지 않았고, `case RESCUE_SHUTTLE_DESCENT:`에도 DESCENT로 전환하는 대체 로직이 보이지 않습니다(`CRIT-4`가 손댄 곳은 `case RESCUE_SHUTTLE:`뿐입니다).
- **판정**: **의심되는 회귀(regression) 가능성.** 주석의 의도와 실제 코드가 어긋나 있어, SHUTTLE_DESCENT 단계에서 목표 고도(`descentAlt`)에 도달한 뒤에도 DESCENT로 넘어가는 경로가 사라졌을 수 있습니다. 다른 파일(예: 별도로 전달되지 않은 gps_rescue.c의 뒷부분)에 보완 코드가 있는지 반드시 재확인이 필요합니다. 지금 diff만 보면 "고친 버그"가 아니라 "새로 만든 버그"일 가능성이 있습니다.

### 2-2. `gps_rescue.c` — FLY_HOME 중 fallback A포인트 생성 시 `takeoffVectorCaptured = true` 추가 (HIGH-3) ⚠️

- **원본**: 이륙 시 A포인트를 못 만들었을 때(즉 `takeoffVectorCaptured == false`) FLY_HOME 도중 임시로 A포인트를 현재 위치로 생성하되, 주석에 "takeoffVectorCaptured는 false 유지 → 아래 CPA 분기 진입 안 함"이라고 명시하며 **의도적으로 false를 유지**했습니다.
- **수정**: 이 fallback 시점에 `takeoffVectorCaptured = true`도 함께 설정.
- **판정**: **원래 설계 의도와 정면으로 배치되는 변경.** 원본 주석은 "이 임시 A포인트는 실제 이륙 벡터가 아니므로 CPA(정밀 통과 판정) 대신 단순 거리 기반 전환을 쓰겠다"는 의도를 분명히 밝히고 있습니다. 수정본은 이 임시 포인트에도 CPA 판정을 적용하게 만드는데, 이게 실제로 더 나은 동작인지 아니면 설계 의도를 깨는 것인지는 코드만으로 단정할 수 없습니다. "버그 수정"이라기보다는 **검증되지 않은 설계 변경**에 가깝습니다.

### 2-3. `gps_rescue.c` — `gpsRescueUpdate()` 상단에 GPS_RESCUE_MODE 비활성 시 즉시 return 추가

- **원본**: `FLIGHT_MODE(GPS_RESCUE_MODE)`가 꺼져 있으면 `gpsRescueStop()`만 호출하고, 이후 `sensorUpdate()`, phase별 switch 등 나머지 로직도 계속 실행됨(즉, 레스큐 모드가 꺼져 있어도 센서/거리 값 등은 계속 갱신됨).
- **수정**: `gpsRescueStop(); rescueAttainPosition(); newGPSData = false; return;`으로 즉시 종료.
- **판정**: **애매함.** 불필요한 연산을 줄이는 최적화로 볼 수도 있지만, 레스큐 모드가 꺼진 상태에서도 OSD 등에 표시되던 거리/헤딩 정보가 더 이상 갱신되지 않는 부작용이 생길 수 있습니다. 원본이 "버그"였다는 근거(주석 등)가 없고, 단순히 동작 범위를 축소시키는 변경이라 실제 버그 수정인지 재검토가 필요합니다.

---

## 

## 
