# 보고서 3 — "심각도 높음" 오류 실재 여부 재검증

> **검증 일자**: 2026-07-21
> **검증 대상**: `/home/betaflight/autopilot` 펌웨어 현재 코드 (커밋 `822f4beaf`)
> **분석 대상 보고서**: `보고서3.md` 제11장 "🔴 심각도 높음: Waypoint 0개 + Aux 1400-1600 상세 분석"
> **방식**: 코드 수정 없음 — 정적 코드 추적만 수행
> **결론 요약**: 보고서 3이 지적한 심각도 높음 문제는 **절반만 실재**한다.
> - **경로 A (IDLE → 직접 Aux 1400-1600)**: ✅ **현재 코드에도 그대로 존재** — 패치 누락 확인됨.
> - **경로 B (활성 Rescue 중 전환)**: ❌ **패치로 이미 해결됨** — 현재 코드에서는 INITIALIZE에 갇히지 않는다.

---

## 1. 보고서 3의 핵심 주장 (재인용)

| 경로 | 주장된 결함 | 보고서 3의 위험도 |
|---|---|---|
| **경로 A** | IDLE 상태에서 Aux 1400-1600 직접 진입 시 `rescueState.phase = RESCUE_MISSION_FLY_WP`가 무조건 오버라이드되어, waypoint 0개여도 `handleMissionPhase()`가 잘못된 좌표((0,0) 또는 누적 좌표)로 비행 → 기체 이탈 | 🔴 매우 높음 |
| **경로 B** | 활성 Rescue(FLY_HOME 등) 중 Aux를 1400-1600으로 변경 시 `missionIsActive()==false`라 phase가 `RESCUE_INITIALIZE`에 갇히고 제어 출력이 고정되어 관성 비행 | 🔴 높음 |

보고서 3의 해결방안 2(§11.6)는 정확히 **경로 A의 IDLE 블록 무조건 오버라이드**를 지적했다.

---

## 2. 현재 코드 추적 결과

### 2.1 경로 A — IDLE 블록 (gps_rescue.c:1271-1289)

```c
} else if (FLIGHT_MODE(GPS_RESCUE_MODE) && rescueState.phase == RESCUE_IDLE) {
    const uint16_t auxVal = getRescueAuxValue();
    if (failsafeIsReceivingRxData() && auxVal < 1400) {
        ... RESCUE_SHUTTLE_INFINITE ...
    } else if (failsafeIsReceivingRxData() && auxVal < 1600) {
        missionStart();     // ★ waypoint 0개 → 내부에서 phase=RESCUE_INITIALIZE 로 fallback
        rescueState.phase = RESCUE_MISSION_FLY_WP;  // ★ 무조건 오버라이드 (guard 없음)
    } else {
        gpsRescueStart();
    }
    rescueAttainPosition();
    performSanityChecks();
}
```

`missionStart()` (mission.c:98-103):

```c
if (missionWpCount == 0) {
    isMissionActive = false;
    rescueState.phase = RESCUE_INITIALIZE;   // ✓ 이미 Rescue로 fallback 함
    return;
}
```

→ `missionStart()`가 `phase=RESCUE_INITIALIZE`로 fallback 시켰음에도, **바로 다음 줄 1282행이 `RESCUE_MISSION_FLY_WP`로 무조건 덮어씁니다.** `missionIsActive()` 체크가 없습니다.

**판정: 보고서 3 경로 A의 결함은 현재 코드에도 실재한다.** (보고서 3 §11.6 해결방안 2가 가리킨 코드가 그대로 남아 있음)

### 2.2 경로 A — 이후 매 루프 동작

`rescueAttainPosition()`은 switch 문 이후 (gps_rescue.c:1587-1588) **매 루프 항상 재호출**되므로, 보고서 3이 "switch에 `RESCUE_MISSION_FLY_WP` case가 없어 `default: break` → 호출 안 됨"이라고 한 분석은 **현재 코드에서는 틀림**. 실제로는 972행 `case RESCUE_MISSION_FLY_WP: handleMissionPhase();` 가 존재하고 매 루프 실행된다.

`handleMissionPhase()` (gps_rescue.c:794-845)는 `currentVCLat/Lon`을 **덮어쓰지 않고 read-only 사용**. 경로 A에서는 `missionUpdateTargetOnly()`가 호출되지 않으므로 `currentVCLat/Lon`은 다음 둘 중 하나:
- 최초 Rescue 활성화 직후라면 전역 초기값 `(0, 0)` (gps_rescue.c:137-138, `= 0`)
- 과거 Rescue 세션의 누적 좌표

어느 쪽이든 **의도된 WP 좌표가 아님** → heading/bank/yaw/pitch/throttle은 정상 계산되지만 **목표 방향이 잘못됨** → 기체가 의도치 않은 방향으로 이탈. 보고서 3의 핵심 위험(이탈)은 타당.

### 2.3 경로 B — 활성 Rescue 중 전환 (gps_rescue.c:1431-1444)

```c
case RESCUE_FLY_HOME:
    if (failsafeIsReceivingRxData()) {
        const uint16_t aux = getRescueAuxValue();
        if (aux < 1400) { ... RESCUE_SHUTTLE_INFINITE ... break; }
#ifdef USE_FLIGHT_PLAN
        if (aux < 1600) {
            missionStart();  // waypoint 0개 → phase=RESCUE_INITIALIZE (fallback)
            if (missionIsActive()) rescueState.phase = RESCUE_MISSION_FLY_WP;  // ★ guard 존재
            break;
        }
#endif
    }
```

→ `missionIsActive()`가 false이므로 `RESCUE_MISSION_FLY_WP` 오버라이드가 **실행되지 않음**. `missionStart()`가 설정한 `phase=RESCUE_INITIALIZE`가 유지됨.

다음 루프 `case RESCUE_INITIALIZE` (gps_rescue.c:1356-1406) 진입:
- `currentVCLat/Lon`을 A포인트 또는 `GPS_home`으로 정상 세팅
- `missionStart()` 재호출 → `missionIsActive` false → `phase=RESCUE_INITIALIZE` 유지
- `if (missionIsActive())` 스킵
- 고도/거리 조건에 따라 `RESCUE_FLY_HOME` 또는 `RESCUE_ATTAIN_ALT`로 **정상 fallback 진입**

**판정: 보고서 3 경로 B의 "INITIALIZE에 갇혀 제어 출력 고정/관성 비행" 주장은 현재 코드에서는 성립하지 않는다.** 패치(경로 B의 `if (missionIsActive())` 가드, §11.6 해결방안 2/3 반영)가 이미 적용되어 있으며, `RESCUE_INITIALIZE` case가 정상적인 FLY_HOME/ATTAIN_ALT fallback 로직을 포함하고 있기 때문.

> 참고: 경로 B의 동일 패턴(`if (missionIsActive())` 가드)은 ATTAIN_ALT(1414-1420), SHUTTLE(1537-1542) 등 모든 재진입 분기에도 일관되게 적용되어 있음을 확인.

---

## 3. 종합 판정

| 항목 | 보고서 3 주장 | 실재 여부 (현재 코드) | 근거 |
|---|---|---|---|
| **경로 A**: IDLE → 직접 Aux 1400-1600, WP 0개 → (0,0) 또는 잘못된 좌표로 이탈 | 🔴 존재 | ✅ **실재 (미해결)** | gps_rescue.c:1280-1283 — `missionStart()` fallback 후에도 `RESCUE_MISSION_FLY_WP` 무조건 오버라이드, `missionIsActive()` 체크 누락 |
| **경로 B**: 활성 Rescue 중 전환 → INITIALIZE 갇힘/제어 정지 | 🔴 존재 | ❌ **해결됨 (비실재)** | gps_rescue.c:1438-1441 등 — `if (missionIsActive())` 가드 적용, INITIALIZE case가 FLY_HOME/ATTAIN_ALT로 정상 fallback |

### 3.1 "조종이 순간적으로 되었다 잃었다 반복" 증상과의 관계

- 보고서 3이 유력 원인으로 든 **경로 A**는 실재하므로, WP 미저장 상태에서 Aux 1400-1600을 건드리면 기체가 잘못된 방향으로 이탈하고 사용자가 Aux를 올렸다 내렸다 하며 반복되는 증상의 원인이 **될 수 있음**.
- 보고서 3이 유력 원인으로 든 **경로 B**(INITIALIZE 갇힘)는 현재 코드에서 해소되었으므로, 이 경로로 인한 "되었다 잃었다"는 **발생하지 않음**.
- 나머지 유력 원인인 **Aux 경계값(1400/1600) 노이즈**는 코드层面的 버그가 아니라 하드웨어/RX 설정 문제이므로 별도 확인 필요.

---

## 4. 권장 조치 (코드 수정 없음 — 사용자 확인용)

1. **경로 A만 남은 실재 버그**: `gps_rescue.c:1282`의 무조건 오버라이드를 `if (missionIsActive())` 로 감싸는 것만으로 해결됨(보고서 3 §11.6 해결방안 2). 이 한 줄만 누락된 상태.
2. WP 미저장 상태에서 Aux 1400-1600 사용 금지 (사용자 운용 수칙).
3. Aux 채널 1400/1600 경계 노이즈 확인 (RX 데드밴드/필터 설정).
4. 경로 B는 패치로 해결되었으므로 추가 조치 불필요 — 단, 실기 비행 테스트로 확인 권장 (보고서 3 마지막 문단의 "테스트는 아직 못함" 상태였음).

---

## 5. 검증 한계 (명시)

- 본 검증은 **정적 코드 추적**이며, 실제 비행/SITL 시뮬레이션은 수행하지 않음.
- 경로 A에서 `currentVCLat/Lon`이 정확히 `(0,0)`인지 과거 누적 좌표인지의 여부는 부팅→이륙→최초 Rescue 활성화 사이의 세팅 흐름을 추가 추적해야 확정 가능하나, **어느 쪽이든 의도된 WP 좌표가 아니므로 결함 성립에는 영향 없음**.
- 보고서 3은 "이미 패치 1회 실시, 테스트는 못함" 상태였음을 명시. 본 검증 결과 경로 B 패치는 반영되었고 경로 A 패치는 미반영임을 확인함.

---

**작성자**: 검증 수행 (Kilo)
**코드 수정**: 없음 (지침 준수)
