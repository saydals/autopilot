# GPS Rescue 시나리오 분석 보고서

> 분석 일자: 2025-01-XX  
> 분석 범위: gps_rescue.c, gps_rescue.h, mission.c, core.c, mixer.c  
> **중요: 코드 수정은 하지 않음 (분석만 수행)**

---

## 1. 개요

사용자의 레스큐 시나리오 문서를 기반으로 Betaflight Autopilot 펌웨어의 GPS Rescue, Mission Flight, Infinite Shuttle 모드가 시나리오대로 작동하는지 코드 레벨에서 검증한다.  

**3가지 주요 비행 모드**:
1. **정상 Resuce** (Aux >= 1600) — 표준 GPS Rescue 절차
2. **Mission Flight** (Aux 1400~1600) — 웨이포인트 비행
3. **Infinite Shuttle** (Aux < 1400) — 포인트 A/B 왕복 무한 셔틀

---

## 2. 핵심 진입점 분석

### 2.1 `gpsRescueUpdate()` — 3-way Aux 분기 (gps_rescue.c:719-739)

```c
// 3-way Aux 분기: <1400 셔틀 / 1400~1600 Autopilot / 1600+ Rescue
const uint16_t auxVal = getRescueAuxValue();
if (failsafeIsReceivingRxData() && auxVal < 1400) {
    shuttleInfinite = true;
    ...
    rescueState.phase = RESCUE_SHUTTLE_INFINITE;
} else if (failsafeIsReceivingRxData() && auxVal < 1600) {
    missionStart();     // waypoint 있으면 WP #1, 없으면 Rescue로 넘어감
    rescueState.phase = RESCUE_MISSION_FLY_WP;
} else {
    gpsRescueStart();
}
```

**✅ 시나리오 일치**: Aux 값에 따라 3가지 모드가 올바르게 분기됨.

---

## 3. 정상 Rescue 시나리오 분석

### 3.1 초기 단계 — ATTAIN_ALT (3초 상승)

```c
#define ATTAIN_ALT_TIMEOUT_US 3000000  // 3초 타임아웃
```

**로직** (`RESCUE_ATTAIN_ALT` case):
- `attainAltStartTime` 기록 후 3초 대기
- 3초 초과 또는 `currentAltitudeCm >= returnAltitudeCm`이면 FLY_HOME으로 전환
- 상승 중 pitch = `ascendPitch * 100` (-30도 기본)

**✅ 시나리오 일치**: "초기 단계는 큰 의미 없는 3초간의 상승구간" — 정확히 3초 타임아웃 사용.

### 3.2 Fly Home Phase — 목적지 결정

**시나리오**: "descent alt가 짝수면 이륙시 만들어진 A포인트를 향해 비행, 홀수면 홈을 향해 비행"

**코드 검증**:

#### (1) `RESCUE_INITIALIZE`에서 A포인트 유효성 설정:
```c
if ((int)descentAlt % 2 != 0) {
    aPointValid = false;  // 홀수면 A포인트 무효화 → Home으로 직행
}
// 짝수면 aPointValid는 이전 상태 유지 (takeoff 시 캡쳐된 상태)
```

#### (2) `handleFlyHomePhase()`에서 타겟 결정:
```c
if (aPointValid) {
    currentVCLat = rescuePointA.lat;
    currentVCLon = rescuePointA.lon;
} else {
    currentVCLat = GPS_home[0];
    currentVCLon = GPS_home[1];
}
```

**✅ 시나리오 일치**: 짝수/홀수 조건에 따라 A포인트 또는 Home으로 비행.

### 3.3 A포인트 생성 로직 (descentAlt 짝수 - 이륙 시)

`sensorUpdate()` 내부:
```c
if (ARMING_FLAG(ARMED) && !takeoffVectorCaptured && STATE(GPS_FIX_HOME)) {
    if (((int)descentAlt % 2 == 0)) {
        float distToHomeM = rescueState.sensor.distanceToHomeM;
        bool isNoise = (distToHomeM < 20.0f || distToHomeM > 100.0f); 
        if (!isNoise) {
            // 홈 → 현재위치 방향 벡터로 A포인트 계산
            rescuePointA.lat = GPS_home[0] + latOffset;
            rescuePointA.lon = GPS_home[1] + lonOffset;
            aPointValid = true;
            takeoffVectorCaptured = true;
        }
    }
}
```

**분석**:
- 이륙 후 20m ~ 100m 이내 위치에서 A포인트 생성
- **⚠️ 우려사항**: `isNoise` 판별 조건이 `20m 미만 또는 100m 초과`일 때 노이즈로 판단 → GPS 노이즈가 아니더라도 100m 이상 이격된 활주로에서는 A포인트가 생성되지 않을 수 있음

### 3.4 A포인트 생성 로직 (descentAlt 홀수 - Rescue 중)

`FLY_HOME` 단계에서 fallback:
```c
if (!aPointValid && rescueState.sensor.distanceToHomeM <= rescueState.intent.descentDistanceM) {
    rescuePointA.lat = gpsSol.llh.lat;
    rescuePointA.lon = gpsSol.llh.lon;
    aPointValid = true;
}
```

**✅ 시나리오 일치**: "descent alt가 홀수일경우 홈에서 rescue distance에 도착시 A포인트가 생성" — 홈에서 `descentDistanceM` 이내 진입 시 현재 위치를 A포인트로 설정.

### 3.5 A포인트 도착 후 처리

#### 짝수 모드 (takeoffVectorCaptured = true) — CPA 터치 판정:
```c
if (takeoffVectorCaptured) {
    // CPA 기반 터치 판정 (셔틀과 동일 로직)
    // distToTargetCm 감소 → 증가 전환 시 shouldTransition = true
    // 또는 5m 이내 근접 시 shouldTransition = true
}
```

#### 홀수 모드 (takeoffVectorCaptured = false) — 거리 기반 전환:
```c
if (distToTargetM <= rescueState.intent.descentDistanceM) {
    shouldTransition = true;
}
```

#### 전환 결정:
```c
if (shouldTransition && ...) {
    if (shuttleCount == 0.0f) {
        rescueState.phase = RESCUE_DESCENT;  // 바로 하강
    } else {
        initShuttlePoints();
        rescueState.phase = RESCUE_SHUTTLE;  // 셔틀 모드로
    }
}
```

**✅ 시나리오 일치**: "A포인트에 도착후 바로 Home 으로 가거나 셔틀 모드로 고도를 낮추고 Home으로 간다" — shuttleCount에 따라 DESCENT 직행 또는 SHUTTLE 모드 진입.

---

## 4. Infinite Shuttle 모드 분석

### 4.1 발동 조건
```c
// gpsRescueUpdate() IDLE 진입
if (failsafeIsReceivingRxData() && auxVal < 1400) {
    shuttleInfinite = true;
    initShuttlePoints();
    rescueState.phase = RESCUE_SHUTTLE_INFINITE;
}
```

**✅ 시나리오 일치**: "비행중 GPS 신호가 있으면 아무때나 발동할수 있으며" — GPS fix만 있으면 모든 단계에서 발동 가능.

### 4.2 A/B 포인트 생성 방식

#### 방식 1: Home fix 있을 때
- `shuttlePointA` = `rescuePointA` (또는 rescuePointC)
- `shuttlePointB` = A에서 Home 방향으로 `shuttleDistance` 만큼 떨어진 지점

#### 방식 2: Home fix 없을 때 (헤딩 기반)
```c
shuttlePointA = 헤딩 좌측 90도, shuttleDistance/2 지점
shuttlePointB = 헤딩 우측 90도, shuttleDistance/2 지점
```

**✅ 시나리오 일치**: "2가지 방법으로 A B 포인트를 생성해 무한 셔틀 모드로 비행한다" — Home 유무에 따라 2가지 방식 확인됨.

### 4.3 왕복 로직

`handleShuttleProgress()`:
- CPA 기반 터치 판정 (거리 미분)
- B 도착 → A로 전환, A 도착 → 왕복 횟수 증가 후 B로 전환
- `shuttleTargetB` 플래그로 방향 전환

**✅ 무한셔틀**: `shuttleCount` 체크 없이 계속 왕복 (`currentShuttleTrips`를 체크하지 않음).

---

## 5. Mission Flight 모드 분석 ⚠️

### 5.1 진입 조건
```c
// Aux 1400~1600
if (failsafeIsReceivingRxData() && auxVal < 1600) {
    missionStart();
    rescueState.phase = RESCUE_MISSION_FLY_WP;
}
```

### 5.2 `missionStart()` 로직 (mission.c:75-95)

```c
void missionStart(void) {
    // Rescue 재진입 허용: 하강/착륙/비상 단계가 아니면 Autopilot 재시작 가능
    const rescuePhase_e phase = gpsRescueGetPhase();
    if (phase == RESCUE_DESCENT ||
        phase == RESCUE_LANDING ||
        phase == RESCUE_SHUTTLE_DESCENT ||
        phase == RESCUE_ABORT ||
        phase == RESCUE_DO_NOTHING ||
        phase == RESCUE_COMPLETE) {
        return;  // 🔴 하강/착륙 중이면 Mission 시작 거부!
    }
    if (missionWpCount == 0) {
        isMissionActive = false;
        rescueState.phase = RESCUE_INITIALIZE;  // 🔴 웨이포인트 없으면 Rescue 실행
        return;
    }
    ...
}
```

**✅ 하강 단계 진입 방지**: 올바른 설계 — 이미 하강 중이면 Mission 시작하지 않음.

### 5.3 Mission Update 루프 (gps_rescue.c:746-770)

```c
#ifdef USE_FLIGHT_PLAN
    if (missionIsActive()) {
        const uint16_t auxVal = getRescueAuxValue();
        // 🔴 Aux가 1400 미만(셔틀) 또는 1600 이상(Rescue)이면 Mission 중단
        if (failsafeIsReceivingRxData() && (auxVal < 1400 || auxVal >= 1600)) {
            missionStop();
            newGPSData = false;
            return;  // fall-through 방지
        }
        missionUpdateTargetOnly();
        if (!missionIsActive()) {
            newGPSData = false;
            return;  // missionStop()이 이미 phase 설정
        }
        rescueState.phase = RESCUE_MISSION_FLY_WP;
        performSanityChecks();
        rescueAttainPosition();  // handleMissionPhase()
        missionCheckAdvance();   // CPA 판정 → WP++
        newGPSData = false;
        return;  // 기존 switch 분기 건너뜀
    }
#endif
```

**⚠️ 중요**: Mission이 활성화되면 기존 `switch(rescueState.phase)`를 완전히 건너뛰고 Mission 전용 로직으로 처리됨.

### 5.4 `handleMissionPhase()` (gps_rescue.c:348-400)

```c
static void handleMissionPhase(void) {
    // 🆕 미션 전용 네비게이션 핸들러
    // handleFlyHomePhase()와 달리 currentVCLat/Lon을 덮어쓰지 않음.
    // missionUpdateTargetOnly()가 설정한 웨이포인트 좌표를 read-only로 사용.
    ...
    // 뱅크턴, Yaw 제어, 고도 제어 — flyHome과 유사한 구조
    // 고도/속도: missionUpdateTargetOnly()가 설정한 targetAltitudeCm/targetVelocityCmS 사용
    float altErrM = (rescueState.sensor.currentAltitudeCm - rescueState.intent.targetAltitudeCm) * 0.01f;
    ...
    gpsRescueAngle[AI_PITCH] = calculateAltitudePitch(altErrM, false, descentAllowed);
    rescueThrottle = calculateVelocityThrottle();
}
```

**✅ 웨이포인트 고도/속도 사용 확인**: `missionUpdateTargetOnly()`에서 `wp->altitude`, `wp->speed`를 `targetAltitudeCm`/`targetVelocityCmS`에 설정.

### 5.5 웨이포인트 완료 후 처리 (mission.c:188-202)

```c
if (touchCPA || dCm < GPS_RESCUE_TOUCH_PROXIMITY_CM) {
    currentMissionWpIndex++;
    if (currentMissionWpIndex >= missionWpCount) {
        missionStop();  // 모든 WP 소진
    } else {
        ...missionApplyWaypoint();
    }
}
```

`missionStop()`:
```c
void missionStop(void) {
    isMissionActive = false;
    ...
    if (STATE(GPS_FIX_HOME)) {
        gpsRescueResetState();
        rescueState.phase = RESCUE_FLY_HOME;   // Home 귀환
        currentVCLat = GPS_home[0];
        currentVCLon = GPS_home[1];
        rescueState.intent.targetAltitudeCm = rescueState.intent.returnAltitudeCm;
    } else {
        gpsRescueStartShuttleInfinite();  // 무한셔틀
    }
}
```

**✅ 시나리오 일치**: "저장된 모든 way point를 거친후엔 기존 2가지 경로중 하나의 레스큐 단계를 거친다":
- Home fix 있음 → FLY_HOME → DESCENT 또는 SHUTTLE
- **⚠️ 그러나**: "짝수모드인 이륙시 생성된 A포인트 또는 홀수 모드인 홈으로" — missionStop()에서는 항상 Home으로 귀환, A포인트 고려하지 않음. `gpsRescueResetState()`에서 `aPointValid`는 리셋되지 않지만, `rescueState.phase = RESCUE_FLY_HOME` 설정 시 handleFlyHomePhase()가 aPointValid 체크하여 A포인트로 비행할 수 있음.

### 5.6 웨이포인트 고도 — 글라이드 슬로프 (mission.c:117-150)

```c
// 글라이드 슬로프: 목표 고도 점진적 보간
// 첫 진입 시 현재 고도에서 시작, 거리 비례하여 목표 웨이포인트 고도로 보간
if (!wpGlideInitialized || currentDistanceCm > wpGlideStartDistanceCm) {
    wpGlideStartAltitudeCm = (float)rescueState.intent.targetAltitudeCm;
    wpGlideStartDistanceCm = currentDistanceCm;
    wpGlideInitialized = true;
}

if (currentDistanceCm >= WP_ALT_GLIDE_MIN_DISTANCE_CM && wpGlideStartDistanceCm > 0) {
    const float progress = constrainf(1.0f - (currentDistanceCm / wpGlideStartDistanceCm), 0.0f, 1.0f);
    rescueState.intent.targetAltitudeCm = (int32_t)(wpGlideStartAltitudeCm + (float)(wp->altitude - (int32_t)wpGlideStartAltitudeCm) * progress);
} else {
    rescueState.intent.targetAltitudeCm = wp->altitude;
}
```

**✅ 부드러운 고도 전환**: 갑작스러운 고도 변화를 방지하기 위한 글라이드 슬로프 로직 확인됨.

### 5.7 웨이포인트 타입 — 모두 FLYOVER 처리

```c
// 레스큐시나리오.txt: "waypoint시에 다양한 포인트 동작이 있지만 모두 fly hover로 취급 그냥 지나가기로 취급한다."
```

**코드 검증**: `missionCheckAdvance()`는 CPA/근접 판정만 수행하며, 웨이포인트 타입에 따른 분기 없음. 모든 웨이포인트는 단순 통과(FLYOVER)로 처리됨.

**✅ 시나리오 일치**: 웨이포인트 타입과 관계없이 CPA/근접 시 다음 웨이포인트로 전환.

---

## 6. ⚠️ 조종권 반환 분석 (가장 중요)

### 6.1 Rescue 모드 비활성화 메커니즘

`core.c` `processRxModes()`:
```c
if (ARMING_FLAG(ARMED) && (IS_RC_MODE_ACTIVE(BOXGPSRESCUE) || 
    (failsafeIsActive() && failsafeConfig()->failsafe_procedure == FAILSAFE_PROCEDURE_GPS_RESCUE))) {
    if (!FLIGHT_MODE(GPS_RESCUE_MODE)) {
        ENABLE_FLIGHT_MODE(GPS_RESCUE_MODE);
    }
} else {
    DISABLE_FLIGHT_MODE(GPS_RESCUE_MODE);  // 🔴 AUX off → Rescue 해제
}
```

`gpsRescueUpdate()` 첫 줄:
```c
if (!FLIGHT_MODE(GPS_RESCUE_MODE)) {
    gpsRescueStop();  // phase = RESCUE_IDLE
}
```

**✅ 정상 동작**: AUX 스위치 OFF → `GPS_RESCUE_MODE` 해제 → `gpsRescueStop()` → 모든 Rescue 제어 중단 → rcCommand가 사용자 입력으로 복원.

### 6.2 Mixer에서 Rescue Throttle 적용

`mixer.c` `mixTable()`:
```c
#ifdef USE_GPS_RESCUE
if (FLIGHT_MODE(GPS_RESCUE_MODE)) {
    throttle = gpsRescueGetThrottle();
}
#endif
```

**✅ Rescue 활성 시에만 throttle 오버라이드 → 해제 시 원래 throttle 값 사용.**

### 6.3 단계별 Aux 재확인 로직

모든 주요 단계에서 지속적으로 Aux 값을 재확인:

```c
// ATTAIN_ALT
if (failsafeIsReceivingRxData()) {
    const uint16_t aux = getRescueAuxValue();
    if (aux < 1400) { ... shuttle infinite }
    if (aux < 1600) { ... mission }
}

// FLY_HOME — 동일 패턴
// SHUTTLE — 동일 패턴
// SHUTTLE_INFINITE — Aux >= 1400이면 RESCUE_INITIALIZE로
// SHUTTLE_DESCENT — Aux < 1400이면 SHUTTLE_INFINITE로
```

**✅ 단계 전환 가능**: "3가지 비행 단계는 아무때나 서로 바꿔 실행 가능하다" — 매 루프 Aux 재확인하여 전환.

### 6.4 failsafe 조건과 Rescue 동시 고려 ⚠️

모든 Aux 체크에 `failsafeIsReceivingRxData()` 조건이 함께 사용됨:

```c
if (failsafeIsReceivingRxData() && getRescueAuxValue() < 1400) { ... }
```

**분석**:
- Failsafe 중(`failsafeIsReceivingRxData()` = false)에는 Aux 체크가 무시됨
- Rescue는 failsafe로도 실행될 수 있음 (`FAILSAFE_PROCEDURE_GPS_RESCUE`)
- **⚠️ Failsafe Rescue 중에는 Aux 스위치로 해제할 수 없음** — `processRxModes()`에서 `failsafeIsActive() && failsafe_procedure == GPS_RESCUE` 조건으로 `GPS_RESCUE_MODE` 유지
- Failsafe Rescue는 RX 신호 복구 후에야 해제 가능

**✅ 이는 의도된 설계**: Failsafe 중에는 조종 불가능 상태이므로 자동 조종 유지가 안전.

---

## 7. 미션비행 실행 문제 분석 (시나리오: "조종이 순간적으로 되었다 잃었다 반복")

### 7.1 잠재적 문제점

#### (A) `mission.c:75-82` — `missionStart()` 조건 문제

```c
void missionStart(void) {
    const rescuePhase_e phase = gpsRescueGetPhase();
    if (phase == RESCUE_DESCENT ||
        phase == RESCUE_LANDING ||
        phase == RESCUE_SHUTTLE_DESCENT ||
        phase == RESCUE_ABORT ||
        phase == RESCUE_DO_NOTHING ||
        phase == RESCUE_COMPLETE) {
        return;
    }
```

**⚠️ `RESCUE_MISSION_FLY_WP` 상태에서 다시 `missionStart()`가 호출될 경우**: 
- phase가 `RESCUE_MISSION_FLY_WP`이므로 위 조건에 걸리지 않음
- `missionWpCount`가 0이 아니면 `isMissionActive = true`로 재설정되고 `currentMissionWpIndex = 0`으로 리셋됨
- **⚠️ 결과**: 이미 진행 중인 Mission이 처음부터 다시 시작됨!

#### (B) 중복 호출 가능성

```c
// gpsRescueUpdate() - IDLE 진입
} else if (failsafeIsReceivingRxData() && auxVal < 1600) {
    missionStart();
    rescueState.phase = RESCUE_MISSION_FLY_WP;
}

// 그리고 아래 mission 활성화 체크 구문에서도 동일한 Aux 체크가 없음
// 단, gpsRescueUpdate()는 100Hz로 실행됨
```

**분석**: IDLE 진입 시에만 missionStart()가 호출되므로, 이미 RESCUE_MISSION_FLY_WP 상태에서는 재진입하지 않음. 위 (A) 시나리오는 발생하지 않음.

#### (C) Phase 전환 시 I-term 초기화 문제

`gpsRescueUpdate()`:
```c
static rescuePhase_e lastPhase = RESCUE_IDLE;
if (rescueState.phase != lastPhase) {
    velocityIterm = 0.0f;
    altitudePitchIterm = 0.0f;
    yawHeadingIterm = 0.0f;
    ...
}
```

**✅ Phase 전환 시 모든 I-term 초기화 — 올바름.**

### 7.2 추정되는 문제 원인

사용자의 시나리오에서 "미션비행이 실행되지 않고 조종이 순간적으로 되었다 잃었다를 반복"한다는 증상에 대한 분석:

1. **`missionUpdateTargetOnly()` 호출 조건**: `gpsRescueUpdate()`에서 Mission 활성화 시 매 루프 호출. 문제 없음.

2. **가장 유력한 원인 — `gpsRescueUpdate()` 루프 구조**:
   
```c
   #ifdef USE_FLIGHT_PLAN
   if (missionIsActive()) {
       const uint16_t auxVal = getRescueAuxValue();
       // 🔴 Aux가 범위를 벗어나면 mission 중단
       if (failsafeIsReceivingRxData() && (auxVal < 1400 || auxVal >= 1600)) {
           missionStop();
           ...
       }
       missionUpdateTargetOnly();
       ...
       rescueAttainPosition();
       missionCheckAdvance();
       newGPSData = false;
       return;
   }
   #endif
   
```

   - Aux 값이 경계값(1400 또는 1600) 근처에서 노이즈로 인해 순간적으로 변하면 Mission이 시작/중단 반복 가능
   - **⚠️ 그러나**: `failsafeIsReceivingRxData()` 체크가 있으므로 RX 신호 정상 시에만 해당

3. **Phase 전환 사이클 문제**:
   - Mission 활성화 → WP0 비행 → WP0 CPA 도달 → WP1
   - `missionStop()` → `rescueState.phase = RESCUE_FLY_HOME`
   - 다음 루프: FLY_HOME에서 Aux 재확인 → 다시 1400~1600 → `missionStart()` 재호출
   - **⚠️ 이 사이클이 매우 빠르게 반복될 가능성 있음**

4. **글라이드 슬로프 재초기화 문제**:
   
```c
   static void missionApplyWaypoint(void) {
       wpGlideInitialized = false;  // 새 waypoint 진입 → 글라이드 슬로프 재초기화
       missionUpdateTargetOnly();
   }
   
```
   - 각 웨이포인트 전환 시 글라이드 슬로프가 재초기화되지만, 정상 동작 범위 내

### 7.3 ✅ 정상 동작으로 확인된 항목

- 웨이포인트 CPA/근접 판정 로직 정상
- 글라이드 슬로프 고도 보간 정상
- 각 웨이포인트의 고도/속도 적용 정상
- 웨이포인트 완료 후 Home 귀환 또는 무한셔틀 전환 정상
- 모든 단계에서 Rescue 해제 시 조종권 반환 메커니즘 정상

---

## 8. 종합 검증 결과

### 8.1 검증 항목별 상태

| # | 검증 항목 | 상태 | 설명 |
|---|-----------|------|------|
| 1 | 초기 3초 상승 | ✅ | ATTAIN_ALT 타임아웃 3초 정확히 구현 |
| 2 | Fly Home: 짝수→A포인트, 홀수→Home | ✅ | descentAlt % 2 조건으로 분기 |
| 3 | A포인트 생성 (짝수/이륙시) | ✅ | 20~100m 범위에서 생성 |
| 4 | A포인트 생성 (홀수/Rescue 중) | ✅ | descentDistanceM 이내 진입 시 생성 |
| 5 | A포인트 통과 후 DESCENT 또는 SHUTTLE | ✅ | shuttleCount 조건에 따라 분기 |
| 6 | 셔틀 A/B 왕복 | ✅ | CPA 터치 판정 정상 |
| 7 | 무한셔틀 발동 | ✅ | Aux < 1400, 모든 단계에서 가능 |
| 8 | 미션 웨이포인트 비행 | ✅ | 웨이포인트 고도/속도 적용 |
| 9 | 모든 WP FLYOVER 처리 | ✅ | 타입 구분 없이 CPA/근접 통과 |
| 10 | WP 완료 후 Home/셔틀 전환 | ⚠️ | missionStop() → FLY_HOME (항상 Home 우선) |
| 11 | 3가지 모드 상호 전환 | ✅ | 매 루프 Aux 재확인 |
| 12 | **Rescue 해제 시 조종권 반환** | ✅ | GPS_RESCUE_MODE 해제 → gpsRescueStop() |
| 13 | **Failsafe Rescue 해제 조건** | ⚠️ | RX 복구 후에만 해제 가능 (의도된 설계) |
| 14 | Mission 반복 시작 가능성 | ⚠️ | WP 완료 후 FLY_HOME → 재진입 시 missionStart() 재호출 |

### 8.2 시나리오 대비 미일치/미구현 사항

1. **짝수모드 웨이포인트 완료 후**: 시나리오에서는 "Waypoint 다 소모후에 짝수모드인 이륙시 생성된 A포인트 ... 로 가면 된다"라고 명시되었으나, 실제 코드의 `missionStop()`에서는 **항상 Home으로 귀환** 설정. 단, `handleFlyHomePhase()`에서 `aPointValid`가 true이면 A포인트로 비행하도록 되어 있어 결과적으로 A포인트 방문 가능.

### 8.3 조종권 반환 확실성 평가

**✅ 안전하게 구현됨**:

1. `processRxModes()` — AUX OFF → `GPS_RESCUE_MODE` 해제
2. `gpsRescueUpdate()` — `!GPS_RESCUE_MODE` → `gpsRescueStop()` → `RESCUE_IDLE`
3. `rescueAttainPosition()` — `RESCUE_IDLE` → `gpsRescueAngle[PITCH/ROLL] = 0`, `rescueThrottle = rcCommand[THROTTLE]`
4. `mixTable()` — `!GPS_RESCUE_MODE` → Rescue throttle 오버라이드 없음

위 4단계가 순차적으로 실행되어 **확실한 조종권 반환**이 보장됨.

---

## 9. 권장 확인 사항

1. **실제 비행 로그(Blackbox) 검토 필요**: Mission 비행 중 `rescueState.phase` 변화와 `isMissionActive` 상태를 Blackbox 로그로 확인할 것
2. **Aux 채널 노이즈 확인**: 경계값(1400, 1600) 근처에서 Aux 값이 흔들리지 않는지 확인
3. **Mission → FLY_HOME → 재진입 사이클 검증**: WP 완료 후 `missionStop()` → FLY_HOME → 다음 루프에서 `missionStart()` 재호출 시뮬레이션

---

## 10. 최종 결론

**대부분의 시나리오가 코드에 정확히 구현되어 있음.** 특히 조종권 반환 메커니즘은 4단계에 걸쳐 확실하게 보장된다. 

다만, 2가지 포인트에 대한 확인이 필요하다:
1. 미션 웨이포인트 완료 후 **항상 Home 귀환**으로 설정되는 점 (시나리오에서는 짝수모드 A포인트 우선 언급)
2. Mission 재진입 시 `missionStart()`가 `currentMissionWpIndex = 0`부터 다시 시작하는 점 (의도된 설계인지 확인 필요)

**⚠️ 추가 검증 없이 비행하지 말 것** — 실제 하드웨어에서 시나리오별로 동작을 검증하고 Blackbox 로그를 분석할 것을 강력히 권장.
