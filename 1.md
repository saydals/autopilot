# velocityToHomeCmS 분석 보고서

## 1. velocityToHomeCmS 계산 (sensorUpdate 함수)

`rescueState.sensor.velocityToHomeCmS`는 **HOME 방향**으로의 이동 속도를 나타냅니다:

```c
rescueState.sensor.velocityToHomeCmS = ((prevDistanceToHomeCm - rescueState.sensor.distanceToHomeCm) / rescueState.sensor.gpsDataIntervalSeconds);
```

- 이전 프레임의 홈까지 거리 - 현재 프레임의 홈까지 거리 = 시간당 홈 방향 접근 거리
- 즉, **항상 "HOME" 기준**으로 계산됨. 현재 타겟(셔틀포인트/웨이포인트)이 무엇이든 관계없이 홈 방향 속도만 측정.

## 2. velocityToHomeCmS 사용처 (performSanityChecks)

```c
if (rescueState.phase == RESCUE_FLY_HOME || rescueState.phase == RESCUE_MISSION_FLY_WP) {
    const float velocityToHomeCmS = rescueState.sensor.velocityToHomeCmS;
    rescueState.intent.secondsFailing += (velocityToHomeCmS < 0.1f * rescueState.intent.targetVelocityCmS) ? 1 : -1;
    ...
    if (rescueState.intent.secondsFailing >= 30) {
        rescueState.failure = RESCUE_FLYAWAY;
    }
}
```

**이 체크는 RESCUE_FLY_HOME 과 RESCUE_MISSION_FLY_WP 에서만 실행됩니다.**

## 3. 각 Phase별 분석

### A. 셔틀 비행 (RESCUE_SHUTTLE, RESCUE_SHUTTLE_INFINITE, RESCUE_SHUTTLE_DESCENT)

**velocityToHomeCmS 체크가 적용되지 않음** → 안전!

- performSanityChecks()의 if 조건에 셔틀 phase가 포함되어 있지 않음
- 셔틀이 홈 방향과 수직/반대로 비행해도 secondsFailing이 증가하지 않음
- 셔틀 카운트 완료 후 SHUTTLE_DESCENT → DESCENT (홈방향) 으로 정상 전환됨

### B. 미션 웨이포인트 비행 (RESCUE_MISSION_FLY_WP)  **🔴 문제 발생!**

**velocityToHomeCmS 체크가 적용됨 → 심각한 문제**

- 웨이포인트가 홈 방향과 다른 방향이면 velocityToHomeCmS가 음수 또는 매우 작은 값이 됨
- `velocityToHomeCmS < 0.1f * targetVelocityCmS` → 참 (velocityToHome이 목표속도의 10% 미만)
- secondsFailing이 1초에 1씩 증가
- **30초 후 RESCUE_FLYAWAY → RESCUE_DO_NOTHING → 추락**

**실제 시나리오**: 
- 웨이포인트 #1이 홈에서 200m 떨어진 곳에 있음
- 기체가 웨이포인트 #1로 비행 중 → 홈과 멀어짐 → velocityToHomeCmS = 음수
- 30초 후 flyaway 판정 → 레스큐 실패

## 4. 사용자의 가설 검증

**Q: "다양한 home 방향 목적이 아닌 비행시 shuttle(무한,하강)비행과 waypoint 비행중에는 velocityToTargetCmS 가 필요하다"**

✅ **셔틀**: velocityToHomeCmS 체크가 아예 적용되지 않으므로 문제없음. 다만 velocityToTargetCmS가 없어도 동작에는 문제없지만, 만약 셔틀중 기체가 멈추거나 제자리 선회하는 상황 감지가 안됨.

✅ **웨이포인트**: **심각한 문제 있음**. velocityToHomeCmS 체크가 적용되어 웨이포인트가 홈과 다른 방향이면 30초 후 무조건 Flyaway 판정.

**Q: "velocityToTargetCmS 가 필요하고 이것을 만족하지 못하면 홈이 아닌 타겟 방향은 포기하고 홈방향으로 가는 정상레스큐로 돌린다"**

✅ 정확한 제안. 현재 코드는 MISSION_FLY_WP에서 velocityToTargetCmS 체크 없이 velocityToHomeCmS로 검증하므로 홈 아닌 방향 웨이포인트에서 오작동함.

올바른 로직:
1. MISSION_FLY_WP: velocityToTargetCmS (웨이포인트 방향) 체크
2. 목표방향 속도 부족 → RESCUE_FLY_HOME 으로 fallback
3. RESCUE_FLY_HOME: velocityToHomeCmS 체크
4. 홈방향 속도도 부족 → RESCUE_FLYAWAY (현행 유지)

## 5. 코드 수정 난이도 평가

### 난이도: 중간 (Medium)

### 필요한 변경사항:

1. **rescueSensorData_s 구조체에 velocityToTargetCmS 추가** (gps_rescue.h)
   - 간단한 필드 추가

2. **sensorUpdate()에 velocityToTargetCmS 계산 추가** (gps_rescue.c)
   - 현재 타겟 좌표(currentVCLat/currentVCLon) 기준 거리 계산 필요
   - prevDistanceToTargetCm 변수 추가 필요 (prevDistanceToHomeCm 처럼)

3. **performSanityChecks() 수정** (gps_rescue.c)
   - MISSION_FLY_WP: velocityToTargetCmS 체크로 변경
   - 체크 실패 시 secondsFailing 대신 RESCUE_FLY_HOME 전환
   - 셔틀 phase에도 velocityToTargetCmS 체크 추가 (옵션)

4. **Phase 전환 로직 추가**
   - MISSION_FLY_WP 실패 → RESCUE_FLY_HOME 전환 (missionStop() 호출)
   - 이때 intent.targetAltitudeCm/resetVelocity 등 적절히 초기화

### 예상 코드 변경량:
- gps_rescue.h: 2~3줄 추가
- gps_rescue.c: 30~50줄 추가/수정
- mission.c: 영향 없음 (rescueState 직접 접근 안함)

### 리스크:
- RESCUE_FLY_HOME fallback 시 셔틀/미션 상태 정리 누락 위험
- Phase 전환 시 I-term, smoothedPitch 등 초기화 필요 (기존 코드에 있음)
- velocityToTargetCmS 계산 시 GPS 1Hz 노이즈 영향 (velocityToHomeCmS와 동일)

### 권장 접근법:
1. `velocityToTargetCmS` 만 추가하고 fallback 로직은 Phase 전환으로 처리
2. 테스트: 셔틀 + 웨이포인트 시나리오 각각 검증
3. 30초 타임아웃 유지 또는 웨이포인트 특성에 맞게 조정

---

**결론**: 
- 셔틀 비행은 velocityToHomeCmS 체크 대상이 아니라서 현재 코드에서 문제없음
- **웨이포인트 비행(RESCUE_MISSION_FLY_WP)은 velocityToHomeCmS 체크 대상이라서 웨이포인트가 홈 아닌 방향이면 무조건 30초 후 Flyaway abort 발생 → 심각한 버그**
- 셔틀에 velocityToTargetCmS를 추가하는 것은 선택사항 (안전성 향상 목적)
- 웨이포인트에 velocityToTargetCmS를 추가하는 것은 **필수 수정사항**
- 코드 수정 난이도: 중간 (구조 변경보다는 기존 패턴 따르는 로직 추가)
