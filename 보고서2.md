# GPS Rescue 시나리오 분석 보고서 V2 — 심층 코드 검증

> **분석 일자**: 2025-01-XX  
> **분석 대상 파일**:
> - `src/main/flight/gps_rescue.c` / `.h` — Rescue 상태 머신 및 제어 로직
> - `src/main/flight/mission.c` / `.h` — 미션 웨이포인트 비행
> - `src/main/fc/rc.c` — RC 명령 처리 및 Rescue Yaw 오버라이드
> - `src/main/fc/core.c` — 메인 루프, 모드 활성화, Disarm
> - `src/main/fc/pid.c` — PID 제어기, Angle 모드 + Rescue 각도 주입
> - `src/main/flight/mixer.c` — Mixer 테이블, Rescue Throttle 오버라이드
> - `src/main/flight/failsafe.c` — Failsafe 상태 머신, GPS Rescue 진입/해제
> - `src/main/pg/gps_rescue.c` — PG 설정 저장소
> - **코드는 절대 수정하지 않음 — 분석만 수행**

---

## 목차
1. [시스템 아키텍처 개요](#1-시스템-아키텍처-개요)
2. [RC 명령 → Rescue 명령 데이터 흐름](#2-rc-명령--rescue-명령-데이터-흐름)
3. [상태 천이 그래프 (State Transition Graph)](#3-상태-천이-그래프)
4. [단계별 심층 분석](#4-단계별-심층-분석)
5. [미션 모드 심층 분석](#5-미션-모드-심층-분석)
6. [조종권 반환 체인 분석](#6-조종권-반환-체인-분석)
7. [타이밍 및 스케줄링 이슈](#7-타이밍-및-스케줄링-이슈)
8. [엣지 케이스 및 잠재적 버그](#8-엣지-케이스-및-잠재적-버그)
9. [종합 검증 매트릭스](#9-종합-검증-매트릭스)
10. [결론 및 권장사항](#10-결론-및-권장사항)

---

## 1. 시스템 아키텍처 개요

### 1.1 모듈 간 데이터 흐름

```
[RC 수신기] → rc.c:updateRcCommands() → rcCommand[] (raw)
                                              ↓
             rc.c:processRcCommand() → setpointRate[], rcDeflection[]
                    ↓ YAW 오버라이드              ↓ ROLL/PITCH
                    (gpsRescueGetYawRate)         (gpsRescueAngle)
                    ↓                                ↓
             pid.c:pidController() ← pidLevel()에 gpsRescueAngle 주입
                    ↓
             pidData[axis].Sum (P+I+D+F)
                    ↓
             mixer.c:mixTable()
                    ↓ THROTTLE 오버라이드
                    (gpsRescueGetThrottle when GPS_RESCUE_MODE)
                    ↓
             motor[] → ESC
```

### 1.2 Rescue 제어가 주입되는 3개 레이어

| 레이어 | 파일 | 함수 | 오버라이드 대상 | 조건 |
|--------|------|------|----------------|------|
| **Layer 1: RC 입력** | `rc.c` | `processRcCommand()` | YAW setpoint | `FLIGHT_MODE(GPS_RESCUE_MODE)` |
| **Layer 2: PID 제어** | `pid.c` | `pidLevel()` | ROLL/PITCH angle target | `FLIGHT_MODE(GPS_RESCUE_MODE)` |
| **Layer 3: Mixer 출력** | `mixer.c` | `mixTable()` | THROTTLE | `FLIGHT_MODE(GPS_RESCUE_MODE)` |

### 1.3 Layer 1 상세: rc.c `processRcCommand()` 中 Yaw 오버라이드

```c
// rc.c line 387-396
#ifdef USE_GPS_RESCUE
if ((axis == FD_YAW) && FLIGHT_MODE(GPS_RESCUE_MODE)) {
    // GPS Rescue 활성 시 Yaw setpoint를 Rescue Yaw rate로 대체
    angleRate = gpsRescueGetYawRate();
    // 스틱 입력을 중앙(0)으로 간주 → acceleration limit 등의 변형 방지
    rcDeflection[axis] = 0;
    rcDeflectionAbs[axis] = 0;
} else
#endif
```

**의미**: GPS Rescue 중 Yaw 스틱은 완전히 무시됨. `rcDeflection=0`이므로 `accelerationLimit()`, feedforward 계산에 영향 없음.

### 1.4 Layer 2 상세: pid.c `pidLevel()` 中 Rescue Angle 주입

```c
// pid.c line 145-146
#ifdef USE_GPS_RESCUE
angleTarget += gpsRescueAngle[axis] / 100.0f; // centidegrees → degrees
#endif
```

**의미**: 
- `gpsRescueAngle[AI_ROLL]` — Roll 각도 목표값 (rescue에서 계산한 bank angle)
- `gpsRescueAngle[AI_PITCH]` — Pitch 각도 목표값 (rescue에서 계산한 pitch)
- 이 값들은 `ANGLE_MODE`가 활성화된 것처럼 `pidLevel()` 내에서 angle error 계산에 사용됨
- GPS_RESCUE_MODE일 때 `pidController()`는 강제로 ANGLE_MODE 경로를 통해 pidLevel() 호출

### 1.5 Layer 3 상세: mixer.c `mixTable()` 中 Throttle 오버라이드

```c
// mixer.c line 273-275
#ifdef USE_GPS_RESCUE
if (FLIGHT_MODE(GPS_RESCUE_MODE)) {
    throttle = gpsRescueGetThrottle();
}
#endif
```

**의미**: 
- Rescue 활성 시 throttle은 `gpsRescueGetThrottle()`이 반환하는 값으로 완전 대체
- `gpsRescueGetThrottle()`은 `rescueThrottle`을 PWM 범위에서 0.0~1.0으로 스케일링
- `rescueThrottle`은 각 phase handler에서 `calculateVelocityThrottle()`로 계산됨

---

## 2. RC 명령 → Rescue 명령 데이터 흐름

### 2.1 정상 조종 시 데이터 경로

```
RC 수신기 → rcData[] → rcCommand[] → setpointRate[] → pidController()
                                                              ↓
                                                         pidData[].Sum
                                                              ↓
                                                         mixTable()
                                                              ↓
                                                         motor[]
```

### 2.2 Rescue 활성 시 데이터 경로

```
gpsRescueUpdate() → rescueAttainPosition() → handleXXXXPhase()
                                                   ↓
                                             gpsRescueAngle[ROLL/PITCH] 계산
                                             rescueThrottle 계산
                                             rescueYaw 계산
                                                   ↓
rc.c: processRcCommand() → YAW = gpsRescueGetYawRate() ← rescueYaw
                                                   ↓
pid.c: pidLevel() → angleTarget += gpsRescueAngle[axis] / 100
                                                   ↓
pid.c: pidController() → pidData[].Sum = PID(angleTarget - currentAngle)
                                                   ↓
mixer.c: mixTable() → throttle = gpsRescueGetThrottle() ← rescueThrottle
                                                   ↓
                                             motor[]
```

### 2.3 Rescue 해제 시 데이터 경로 (조종권 반환)

```
AUX Switch OFF
      ↓
core.c: processRxModes() → DISABLE_FLIGHT_MODE(GPS_RESCUE_MODE)
      ↓
gpsRescueUpdate() → if(!GPS_RESCUE_MODE) gpsRescueStop() → phase = RESCUE_IDLE
      ↓
rescueAttainPosition() → case RESCUE_IDLE:
    gpsRescueAngle[AI_PITCH] = 0;
    gpsRescueAngle[AI_ROLL] = 0;
    rescueThrottle = rcCommand[THROTTLE];  // ← 사용자 throttle으로 복원
    lastRescueYaw = 0;
      ↓
rc.c: !GPS_RESCUE_MODE → Yaw 정상 복원
      ↓
pid.c: !GPS_RESCUE_MODE → angleTarget 정상 복원
      ↓
mixer.c: !GPS_RESCUE_MODE → throttle = rcCommand[THROTTLE] (정상)
```

---

## 3. 상태 천이 그래프 (State Transition Graph)

### 3.1 Rescue 정상 모드 상태 천이

```
RESCUE_IDLE
    ↓ (GPS_RESCUE_MODE ON + Aux >= 1600)
RESCUE_INITIALIZE
    ├── GPS_FIX_HOME 없음 → GPS_FIX + Aux<1400 → SHUTTLE_INFINITE
    ├── GPS_FIX_HOME 없음 → 그 외 → DO_NOTHING (NO_HOME_POINT)
    ├── 홈 30m 이내 + A포인트 없음 → DO_NOTHING
    ├── 홈 5m 이내 + 착륙고도 이하 → ABORT → DO_NOTHING
    ├── Aux < 1400 → SHUTTLE_INFINITE
    ├── Aux 1400-1600 + waypoint 있음 → MISSION_FLY_WP
    ├── Aux 1400-1600 + waypoint 없음 → ATTAIN_ALT (missionStart이 rescue 실행)
    ├── 현재고도 >= 목표고도 → FLY_HOME
    └── 그 외 → ATTAIN_ALT
            ↓ (3초 or 목표고도 도달)
        FLY_HOME
            │ ← 각 루프에서 Aux 재확인 (SHUTTLE/MISSION/RESCUE 전환 가능)
            │
            ├── A포인트 도착 (CPA) + shuttleCount=0 → DESCENT
            ├── A포인트 도착 (CPA) + shuttleCount>0 → SHUTTLE
            └── A포인트 도착 (CPA) + 셔틀 중 → SHUTTLE_DESCENT
                    ↓ (셔틀왕복 완료 or descentAlt 도달)
                SHUTTLE_DESCENT
                    ↓ (A포인트 도착 + descentAltReached)
                DESCENT
                    │ ← 각 루프에서 Aux 재확인
                    │
                    ├── 급하강 조건 → isDescentFalling → 급하강 → 홈추적
                    └── 정상하강 → 하강률 자동조절
                            ↓ (홈 30m + landingAlt)
                        LANDING
                            ↓ (3초 후 PWM 1000)
                        모터 정지 (disarmOnImpact)
```

### 3.2 무한셔틀 모드 상태 천이

```
어느 단계에서나 → Aux < 1400 → SHUTTLE_INFINITE
        ↓
SHUTTLE_INFINITE
    ├── Aux >= 1400 → RESCUE_INITIALIZE (Rescue 재진입)
    └── 계속 Aux < 1400 → 무한 A/B 왕복
        ↓ (Aux 1400-1600)
    MISSION_FLY_WP (위치/고도/속도 유지하며 WP 비행)
```

### 3.3 미션 모드 상태 천이

```
어느 단계에서나 → Aux 1400-1600 → missionStart() + MISSION_FLY_WP
        ↓
MISSION_FLY_WP (gpsRescueUpdate() 내 특수 경로)
    │
    ├── Aux < 1400 → missionStop() → SHUTTLE_INFINITE (or FLY_HOME if home fix)
    ├── Aux >= 1600 → missionStop() → FLY_HOME (rescue로 복귀)
    │
    ├── WP CPA 도달 → WP Index++
    │       ├── 마지막 WP → missionStop() → FLY_HOME
    │       └── 다음 WP 있음 → WP 적용 → 계속 비행
    │
    └── WP 타임아웃 5분 → WP Skip → 계속
```

---

## 4. 단계별 심층 분석

### 4.1 RESCUE_INITIALIZE — 초기화 단계

```c
case RESCUE_INITIALIZE:
    // A포인트 상태에 따라 타겟 좌표 설정
    currentVCLat = aPointValid ? rescuePointA.lat : GPS_home[0];
    currentVCLon = aPointValid ? rescuePointA.lon : GPS_home[1];

    if (!STATE(GPS_FIX_HOME)) {
        // Home fix 없음
        if (STATE(GPS_FIX) && failsafeIsReceivingRxData() && getRescueAuxValue() < 1400) {
            // GPS fix + Aux<1400 → 헤딩 기반 무한셔틀
            initShuttlePoints();
            phase = RESCUE_SHUTTLE_INFINITE;
        } else {
            failure = RESCUE_NO_HOME_POINT;
            phase = RESCUE_DO_NOTHING;
        }
    } else if (distanceToHomeM < 30.0F && !aPointValid) {
        // 홈 30m 이내 + A포인트 없음 → 아무것도 하지 않음 (안전)
        phase = RESCUE_DO_NOTHING;
    } else if (distanceToHomeM < 5.0f && currentAltitudeCm < targetLandingAltitudeCm) {
        // 홈 5m 이내 + 착륙고도 → ABORT
        phase = RESCUE_ABORT;
    } else {
        initialiseRescueValues();
        
        // Aux 3-way 분기 (INITIALIZE 단계에서도 동일하게 적용)
        if (failsafeIsReceivingRxData() && getRescueAuxValue() < 1400) {
            shuttleInfinite = true; phase = RESCUE_SHUTTLE_INFINITE;
        } else if (failsafeIsReceivingRxData() && getRescueAuxValue() < 1600) {
            missionStart();
            if (missionIsActive()) phase = RESCUE_MISSION_FLY_WP;
        } else {
            // 정상 Rescue
            if (currentAltitudeCm >= returnAltitudeCm) {
                phase = RESCUE_FLY_HOME;  // 바로 FLY_HOME
            } else {
                phase = RESCUE_ATTAIN_ALT;  // 상승 필요
            }
        }
    }
    break;
```

**⚠️ 중요 발견 — `missionStart()` 실패 시 처리**:

```c
if (failsafeIsReceivingRxData() && getRescueAuxValue() < 1600) {
    missionStart();  // waypoint 0개 → isMissionActive = false, phase = RESCUE_INITIALIZE
    if (missionIsActive()) phase = RESCUE_MISSION_FLY_WP;
    // ⚠️ missionStart()이 phase를 RESCUE_INITIALIZE로 설정했지만,
    // 여기서 phase 재할당하지 않음 → RESCUE_INITIALIZE 유지 → 무한루프 가능!
}
```

**정확한 분석**: `missionStart()`에서 waypoint가 0개이면 `isMissionActive = false`로 설정하고 `rescueState.phase = RESCUE_INITIALIZE`를 설정함. 그러면 `if (missionIsActive())` 조건이 false이므로 phase는 RESCUE_INITIALIZE로 유지됨. 다음 루프에서 다시 RESCUE_INITIALIZE 케이스 진입 → 다시 `missionStart()` 호출 → **무한 루프 발생 가능**. 하지만 `initialiseRescueValues()`는 `phase = RESCUE_ATTAIN_ALT` 또는 `RESCUE_FLY_HOME`을 설정하므로, `failsafeIsReceivingRxData() && getRescueAuxValue() < 1600` 조건이 `else` 블록이 아니라 `else if` 구조로 되어 있어 `else` 블록이 실행됨. 

**재검증**: 코드 구조상:
```c
if (failsafeIsReceivingRxData() && getRescueAuxValue() < 1400) {
    // ...
} else if (failsafeIsReceivingRxData() && getRescueAuxValue() < 1600) {
    missionStart();
    if (missionIsActive()) phase = RESCUE_MISSION_FLY_WP;
    // ⚠️ missionIsActive() = false이면 phase 변경 없음 → RESCUE_INITIALIZE 유지
} else {
    // 정상 Rescue → phase 변경됨
}
```

**결론**: Waypoint가 0개이고 Aux가 1400~1600 사이이면 `missionStart()`가 phase를 `RESCUE_INITIALIZE`로 설정하지만, else 블록이 실행되지 않으므로 RESCUE_INITIALIZE가 유지됨. 그러나 `rescueAttainPosition()`에서 `RESCUE_INITIALIZE`는 return만 하므로 PID 출력이 생성되지 않음. 다음 루프에도 같은 조건으로 다시 진입 → **데드락(phase stuck in RESCUE_INITIALIZE)**. 이는 `missionStart()` 호출 후 phase가 변경되지 않아 발생.

### 4.2 FLY_HOME — A포인트 CPA 터치 판정 심층 분석

```c
// gps_rescue.c FLY_HOME case 내부
if (takeoffVectorCaptured) {
    // ── 짝수 descentAlt (이륙 시 캡쳐된 A포인트) ──
    // 셔틀과 동일한 CPA 터치 판정 로직 재사용
    float dCm = (float)distToTargetCm;
    if (dCm < GPS_RESCUE_TOUCH_ACTIVATION_CM) {  // 2000cm = 20m
        if (cpaDistToTargetCm < 0.0f) {
            cpaDistToTargetCm = dCm;
            cpaWasClosing = true;
        } else {
            bool isClosingNow = (dCm < cpaDistToTargetCm - 20.0f); // 20cm hysteresis
            if (!isClosingNow && cpaWasClosing) {
                shouldTransition = true; // CPA 도달!
            }
            cpaWasClosing = isClosingNow;
        }
        cpaDistToTargetCm = dCm;
    }
    // 근접 폴백 (5m 이내)
    if (dCm < GPS_RESCUE_TOUCH_PROXIMITY_CM) {  // 500cm = 5m
        shouldTransition = true;
    }
}
```

**⚠️ CPA 변수 공유 문제**: `cpaDistToTargetCm`과 `cpaWasClosing`은 FLY_HOME과 셔틀/미션에서 **전역 변수로 공유**됨. FLY_HOME에서 CPA가 진행 중일 때 Aux 변경으로 셔틀이나 미션으로 전환되면 CPA 상태가 초기화되지 않고 그대로 사용됨. `lastPhase != phase` 블록에서 CPA 변수들이 리셋되지만, `gpsRescueUpdate()`의 Phase 전환 감지 블록에서 매번 리셋된다.

**리셋 조건 확인**:
```c
if (rescueState.phase != lastPhase) {
    // ...
    shuttleInfinite = false;
    currentShuttleTrips = 0.0f;
    shuttleTargetB = false;
    cpaDistToTargetCm = -1.0f;
    cpaWasClosing = false;
    descentAltReached = false;
    // ...
    lastPhase = rescueState.phase;
}
```

**✅ CPA 변수는 phase 전환 시 리셋됨 → 안전.**

### 4.3 DESCENT — 급하강(isDescentFalling) 로직

Phase 전환 진입 시 급하강 판정:
```c
if (rescueState.phase == RESCUE_DESCENT && lastPhase != RESCUE_DESCENT) {
    if (shuttleCount == 0.0f && rescueState.sensor.currentAltitudeCm > (descentAlt + 15.0f) * 100.0f) {
        isDescentFalling = true;
    }
}
```

**조건**: `shuttleCount == 0` AND 현재고도 > `descentAlt + 15m`

**급하강 단계**:
1. **정렬 단계** (`descentFallAligned = false`): Fly home 방식으로 헤딩 정렬 (error < 5도)
2. **급하강 단계** (`descentFallAligned = true`): 계단식 피치각 적용
   - 고도차 >= 15m: `maxRescueAngle` (기본 45도)
   - 고도차 >= 10m: 30도
   - 고도차 >= 8m: 20도
   - 고도차 >= 6m: 10도
   - 고도차 >= 4m: 5도
   - 그 외: 0도
3. **홈 추적 전환**: `currentAltM <= descentAlt + 5m` → 홈 방향 tracking + 정상 throttle
4. **급하강 종료**: `currentAltM <= descentAlt` → isDescentFalling = false, 정상 하강으로

**⚠️ 급하강 중 Roll 제어 문제**: 
```c
if (currentAltM <= (descentAlt + DESCENT_HOME_TRACK_ALT_M)) {
    // 홈 추적 (롤/요 정상)
    gpsRescueAngle[AI_ROLL] = targetBankDeg * 100.0f;
    rescueYaw = ...;
    rescueThrottle = calculateVelocityThrottle();
} else {
    // 고도 높을 때: 롤=0, 요=0, 쓰로틀=최소
    gpsRescueAngle[AI_ROLL] = 0.0f;
    rescueYaw = 0.0f;
    rescueThrottle = (float)gpsRescueConfig()->throttleMin;
}
```

**고도가 `descentAlt + 5m`보다 높은 급하강 중에는 롤(방향 제어)이 0으로 고정됨.** 즉 기체는 현재 heading으로 직진하며 급하강만 수행. 이는 descentAlt가 홈 근처에 설정되어 있다고 가정하지만, 만약 급하강 시작 지점이 홈에서 멀리 떨어져 있다면 방향 제어 없이 직진하므로 홈에서 크게 벗어날 수 있음.

### 4.4 LANDING — 최종 착륙

```c
case RESCUE_LANDING:
    // 홈 좌표를 타겟으로 설정
    currentVCLat = GPS_home[0];
    currentVCLon = GPS_home[1];
    gpsRescueAngle[AI_PITCH] = landingPitch * 100.0f;  // -10도 (기수 down)
    gpsRescueAngle[AI_ROLL] = 0.0f;
    rescueYaw = 0.0f;

    // 3초간 throttleMin 유지 → 이후 PWM 1000 (모터 정지)
    if (attainAltStartTime == 0) attainAltStartTime = micros();
    if (cmpTimeUs(micros(), attainAltStartTime) >= ATTAIN_ALT_TIMEOUT_US) {  // 3초
        rescueThrottle = PWM_RANGE_MIN;  // 1000
    } else {
        rescueThrottle = (float)gpsRescueConfig()->throttleMin;
    }
    disarmOnImpact();
    break;
```

**⚠️ LANDING 진입 조건이 `distanceToHomeM <= 30.0f && currentAltitudeCm <= landingAlt * 100`** — 즉 홈 30m 이내로 진입하고 landingAlt 이하 고도일 때 LANDING 시작. LANDING 진입 후에는 throttle 최소 → 3초 후 모터 정지(PWM 1000). **홈 30m 이내에서 모터가 정지되므로, 활주로가 아닌 곳에 강하할 가능성 있음.**

---

## 5. 미션 모드 심층 분석

### 5.1 미션 상태 머신 구조

```c
void gpsRescueUpdate(void) {
    // ...
    
    // [경로 A] Mission 활성화 → 별도 처리
    #ifdef USE_FLIGHT_PLAN
    if (missionIsActive()) {
        // 1) Aux 변경 체크 → 범위 벗어나면 missionStop
        if (failsafeIsReceivingRxData() && (auxVal < 1400 || auxVal >= 1600)) {
            missionStop();
            return;
        }
        // 2) 타겟 업데이트
        missionUpdateTargetOnly();
        // 3) Mission 종료 시 return
        if (!missionIsActive()) { return; }
        // 4) Phase 고정 + 네비게이션 + CPA 체크
        rescueState.phase = RESCUE_MISSION_FLY_WP;
        performSanityChecks();
        rescueAttainPosition();     // handleMissionPhase()
        missionCheckAdvance();      // CPA → WP++
        return;  // ← 여기서 return하여 아래 switch 분기 건너뜀!
    }
    #endif

    // [경로 B] Phase 전환 감지 (I-term 초기화 등)
    if (rescueState.phase != lastPhase) { ... }

    // [경로 C] 정상 switch 분기
    switch (rescueState.phase) { ... }
```

**핵심**: Mission 활성화되면 `gpsRescueUpdate()`는 경로 A에서 `return`하여 경로 B, C를 완전히 건너뜀. Mission이 비활성화되면(`missionStop()` or WP 소진) 경로 B, C가 정상 실행됨.

### 5.2 missionStart() 재진입 문제 상세

**시나리오**: 마지막 WP 도달 → `missionStop()` → `phase = RESCUE_FLY_HOME` → `return` → 다음 루프:
1. `missionIsActive() = false` → 경로 A 스킵
2. 경로 B: Phase 전환 감지 (`lastPhase = MISSION_FLY_WP → FLY_HOME`)
3. 경로 C: `switch(phase): case RESCUE_FLY_HOME:`
4. FLY_HOME case에서 Aux 재확인:
   ```c
   if (failsafeIsReceivingRxData()) {
       const uint16_t aux = getRescueAuxValue();
       if (aux < 1400) { SHUTTLE_INFINITE; break; }
       if (aux < 1600) {
           missionStart();  // ← WP 인덱스 0부터 다시 시작!
           if (missionIsActive()) phase = RESCUE_MISSION_FLY_WP;
           break;
       }
   }
   ```

**✅ 시나리오 일치**: "미션 비행 실행하면 다른 단계 어디에서나 레스큐 모드의 초기 작동을 거쳐 fly home단계에서 미션 비행을 수행하면 된다" — WP 완료 후 FLY_HOME에서 Aux 유지 시 Mission 재진입. **단, 이 경우 WP는 처음부터 다시 시작됨.**

### 5.3 missionStart() 하강 단계 차단

```c
void missionStart(void) {
    const rescuePhase_e phase = gpsRescueGetPhase();
    if (phase == RESCUE_DESCENT ||
        phase == RESCUE_LANDING ||
        phase == RESCUE_SHUTTLE_DESCENT ||
        phase == RESCUE_ABORT ||
        phase == RESCUE_DO_NOTHING ||
        phase == RESCUE_COMPLETE) {
        return;  // ← 하강/착륙/비상 단계에서는 Mission 시작 거부
    }
    // ...
}
```

**✅ 안전 설계**: 이미 하강/착륙 중이면 Mission 시작하지 않음. 이로 인해 "조종이 순간적으로 되었다 잃었다 반복" 증상 방지.

### 5.4 글라이드 슬로프 상세

```c
// mission.c missionUpdateTargetOnly()
if (!wpGlideInitialized || currentDistanceCm > wpGlideStartDistanceCm) {
    wpGlideStartAltitudeCm = (float)rescueState.intent.targetAltitudeCm;
    wpGlideStartDistanceCm = currentDistanceCm;
    wpGlideInitialized = true;
}

if (currentDistanceCm >= WP_ALT_GLIDE_MIN_DISTANCE_CM && wpGlideStartDistanceCm > 0) {
    // 거리 비례 보간
    const float progress = constrainf(
        1.0f - (currentDistanceCm / wpGlideStartDistanceCm), 0.0f, 1.0f
    );
    rescueState.intent.targetAltitudeCm = (int32_t)(
        wpGlideStartAltitudeCm + (float)(wp->altitude - (int32_t)wpGlideStartAltitudeCm) * progress
    );
} else {
    rescueState.intent.targetAltitudeCm = wp->altitude;  // 즉시 목표 고도
}
```

**문제점**: 
1. `wpGlideStartAltitudeCm`이 `rescueState.intent.targetAltitudeCm`에서 초기화됨 → 이전 WP의 글라이드 종료 고도가 시작점이 됨
2. 바람으로 인해 거리가 다시 늘어나면(`currentDistanceCm > wpGlideStartDistanceCm`) 글라이드가 리셋됨
3. WP 5m 이내(`WP_ALT_GLIDE_MIN_DISTANCE_CM = 500cm`)에서는 즉시 목표 고도로 점프

### 5.5 미션 CPA 진행 체크

```c
bool missionCheckAdvance(void) {
    // 타임아웃 5분 → 강제 Skip
    if (cmpTimeUs(micros(), wpEntryTime) > MISSION_WP_TIMEOUT_US) {
        currentMissionWpIndex++;
        if (currentMissionWpIndex >= missionWpCount) missionStop();
        else missionApplyWaypoint();
        return true;
    }

    // CPA 판정 (FLY_HOME과 동일 로직)
    const float dCm = (float)rescueState.intent.distanceToTargetCm;
    if (dCm < GPS_RESCUE_TOUCH_ACTIVATION_CM && dCm >= 0) {
        // ... CPA wasClosing 로직 ...
        if (touchCPA || dCm < GPS_RESCUE_TOUCH_PROXIMITY_CM) {
            currentMissionWpIndex++;
            if (currentMissionWpIndex >= missionWpCount) missionStop();
            else missionApplyWaypoint();
            return true;
        }
    } else {
        prevDistCm = -1.0f;  // 범위 밖 리셋
    }
    return false;
}
```

**✅ FLY_HOME과 동일한 CPA 판정 로직 사용** — `GPS_RESCUE_TOUCH_ACTIVATION_CM`(20m) 진입 시 감시 시작, 20cm hysteresis, 5m 근접 폴백.

---

## 6. 조종권 반환 체인 분석

### 6.1 전체 체인 (4단계)

```
[Step 1] core.c:processRxModes()
    AUX=BOXGPSRESCUE OFF 또는 failsafe 해제
    → DISABLE_FLIGHT_MODE(GPS_RESCUE_MODE)
    ↓
[Step 2] gpsRescueUpdate() (매 루프 첫 줄)
    if (!FLIGHT_MODE(GPS_RESCUE_MODE)) {
        gpsRescueStop();  // phase = RESCUE_IDLE
    }
    ↓
[Step 3] rescueAttainPosition()
    case RESCUE_IDLE:
        gpsRescueAngle[AI_PITCH] = 0.0f;
        gpsRescueAngle[AI_ROLL] = 0.0f;
        rescueThrottle = rcCommand[THROTTLE];  // ← 사용자 throttle 복원
        lastRescueYaw = 0.0f;
        return;
    ↓
[Step 4] mixTable()
    if (!FLIGHT_MODE(GPS_RESCUE_MODE)) {
        // Rescue throttle 오버라이드 없음 → rcCommand[THROTTLE] 사용
    }
    ↓
[Step 5] rc.c:processRcCommand()
    if (!FLIGHT_MODE(GPS_RESCUE_MODE)) {
        // 일반 Yaw 처리 → rcCommand[YAW] 사용
    }
```

**✅ 5단계를 거쳐 확실한 조종권 반환 보장.**

### 6.2 failsafe Rescue 해제 조건

```
failsafe.c FAILSAFE_GPS_RESCUE phase:
    if (receivingRxData) {
        if (areSticksActive(failsafeConfig()->failsafe_stick_threshold) || boxFailsafeSwitchWasOn) {
            // 스틱 30% 이상 or BOXFAILSAFE 스위치 ON → Rescue 해제
            phase = FAILSAFE_RX_LOSS_RECOVERED;
            // → GPS_RESCUE_MODE 해제 → FAILSAFE_MODE 해제
        }
    }
```

**⚠️ failsafe Rescue 해제에는 2가지 조건**:
1. **failsafe 스위치 사용**: 스위치 OFF 시 즉시 해제 (`boxFailsafeSwitchWasOn` = true → `receivingRxDataPeriodPreset = 0`)
2. **실제 신호 손실**: RX 신호 복구 후 스틱을 30% 이상 움직여야 해제
   - `failsafeConfig()->failsafe_stick_threshold = 30` (기본값)

**즉, 실제 RX 신호 손실로 인한 failsafe Rescue 중에는**:
- RX 신호가 복구되어도 스틱을 30% 이상 움직이기 전까지 Rescue가 계속됨
- RX 신호 복구 후 `failsafeIsReceivingRxData() = true`가 되고, `areSticksActive(30)` 조건 충족 시 해제

### 6.3 해제 후 복귀 시간

```c
case FAILSAFE_RX_LOSS_RECOVERED:
    DISABLE_FLIGHT_MODE(GPS_RESCUE_MODE);
    DISABLE_FLIGHT_MODE(FAILSAFE_MODE);
    unsetArmingDisabled(ARMING_DISABLED_FAILSAFE);
    phase = FAILSAFE_IDLE;
```

**✅ 즉시 해제**: GPS_RESCUE_MODE가 해제되면 다음 `gpsRescueUpdate()` 호출에서 `gpsRescueStop()` → `RESCUE_IDLE` → 모든 Rescue 제어 중단.

---

## 7. 타이밍 및 스케줄링 이슈

### 7.1 태스크 실행 주기

| 태스크 | 주기 | 설명 |
|--------|------|------|
| `TASK_GPS_RESCUE` | 100Hz (10ms) | `gpsRescueUpdate()` 실행 |
| `TASK_ATTITUDE` | 100Hz or faster | 자세 추정, angle 계산 |
| GPS 데이터 갱신 | ~10Hz (100ms) | GPS position, groundSpeed 등 |
| PID 제어 루프 | 1-8kHz | 모터 출력 계산 |

### 7.2 GPS 10Hz vs Rescue 100Hz 불일치

```c
// sensorUpdate()에서 GPS 데이터 처리
if (!newGPSData) return;  // GPS 새 데이터 없으면 홈 거리/속도 계산 스킵

rescueState.sensor.distanceToHomeCm = GPS_distanceToHomeCm;
rescueState.sensor.velocityToHomeCmS = ((prevDistanceToHomeCm - rescueState.sensor.distanceToHomeCm) / gpsDataIntervalSeconds);
```

- `newGPSData`는 GPS 인터럽트에서 1회 true로 설정됨
- GPS가 ~10Hz로 갱신되므로, `gpsRescueUpdate()`가 100Hz로 실행되어도 실제 GPS 관련 데이터 갱신은 10Hz
- `velocityToHomeCmS`는 GPS 갱신 간 거리 차이로 계산 → GPS 노이즈에 민감

### 7.3 속도 업샘플링 필터

```c
// 속도 업샘플링: GPS 10Hz groundSpeed를 제어 루프에 맞게 보간
rescueState.sensor.groundSpeedCmS = (uint16_t)fmaxf(0.0f, 
    pt3FilterApply(&velocityUpsampleLpf, (float)gpsSol.groundSpeed));
```

**✅ PT3 필터로 GPS 속도를 보간하여 100Hz 제어에 사용. 단, GPS 신호가 끊기면 필터 출력이 0으로 수렴 → `groundSpeedCmS`가 0이 됨 → 속도 유지 불가.**

### 7.4 초기 3초 상승 타이밍

```c
case RESCUE_ATTAIN_ALT:
    if (attainAltStartTime == 0) attainAltStartTime = micros();
    if (cmpTimeUs(micros(), attainAltStartTime) >= ATTAIN_ALT_TIMEOUT_US) {  // 3초
        phase = RESCUE_FLY_HOME;
    }
```

**⚠️ `attainAltStartTime`은 `RESCUE_INITIALIZE`에서 0으로 초기화됨.** ATTAIN_ALT 진입 시 micros()로 기록, 3초 후 FLY_HOME 전환. GPS 속도가 느리면 3초 동안 충분한 고도 확보가 안 될 수 있음.

---

## 8. 엣지 케이스 및 잠재적 버그

### 8.1 🐛 Waypoint 0개 + Aux 1400-1600 → RESCUE_INITIALIZE 데드락

**파일**: `gps_rescue.c` — RESCUE_INITIALIZE case 내부

**조건**:
- `missionWpCount == 0` (저장된 웨이포인트 없음)
- `failsafeIsReceivingRxData() == true`
- `getRescueAuxValue() < 1600` (Aux 1400-1600)

**발생 시퀀스**:
```
1. phase = RESCUE_INITIALIZE
2. missionStart() 호출 → wpCount=0 → phase = RESCUE_INITIALIZE (재설정)
3. missionIsActive() = false → phase는 RESCUE_INITIALIZE 그대로
4. rescueAttainPosition() → RESCUE_INITIALIZE → return (제어 없음)
5. 다음 루프 → 다시 RESCUE_INITIALIZE case → 1번으로 무한 반복
```

**영향**: 기체가 Rescue 진입하지 못하고 RESCUE_INITIALIZE에 갇힘. `rescueAttainPosition()`의 `case RESCUE_INITIALIZE`는 `return`만 하므로 gpsRescueAngle/Throttle/Yaw가 설정되지 않음 → 이전 cycle의 PID 출력이 유지되거나 0이 됨.

**심각도**: **높음** — 웨이포인트가 없는 상태에서 Aux 실수로 1400-1600 범위에 두면 Rescue가 전혀 작동하지 않음.

### 8.2 🐛 missionStop() 호출 후 FLY_HOME에서 초기 A포인트 상태

**파일**: `mission.c missionStop()` → `gps_rescue.c`

```c
void missionStop(void) {
    // ...
    gpsRescueResetState();
    rescueState.phase = RESCUE_FLY_HOME;
    currentVCLat = GPS_home[0];
    currentVCLon = GPS_home[1];
    rescueState.intent.targetAltitudeCm = rescueState.intent.returnAltitudeCm;
}
```

`gpsRescueResetState()`는 `aPointValid`를 리셋하지 않음:
```c
void gpsRescueResetState(void) {
    shuttleInfinite = false;
    currentShuttleTrips = 0.0f;
    shuttleTargetB = false;
    attainAltStartTime = 0;
    cpaDistToTargetCm = -1.0f;
    cpaWasClosing = false;
    descentAltReached = false;
    turnDirectionSign = 0;
    // ⚠️ aPointValid는 유지됨!
}
```

**영향**: Mission 종료 후 FLY_HOME에서 `aPointValid`가 여전히 true이면 `handleFlyHomePhase()`가 rescuePointA를 향해 비행. 이는 의도된 동작일 수 있지만, missionStop이 항상 Home 귀환을 목표로 한다는 주석과 불일치.

### 8.3 🐛 `lastPhase != phase` 블록에서 `shuttleInfinite` 초기화

```c
if (rescueState.phase != lastPhase) {
    // ...
    shuttleInfinite = false;  // ← 모든 Phase 전환 시 false로 리셋!
    // ...
    lastPhase = rescueState.phase;
}
```

**문제**: `gpsRescueUpdate()` 경로 A(Mission)에서 `return`할 때는 이 블록이 실행되지 않음. 경로 A는 `lastPhase`를 업데이트하지 않음! Mission 내에서 phase가 `RESCUE_MISSION_FLY_WP`로 고정되어 있으므로, Mission 활성화 중에는 `lastPhase`가 절대 업데이트되지 않음.

**시나리오**:
1. Mission 시작 → `lastPhase = RESCUE_MISSION_FLY_WP`
2. Mission 종료 → `missionStop()` → `phase = RESCUE_FLY_HOME`
3. 다음 루프: `missionIsActive() = false` → 경로 A 스킵
4. 경로 B: `rescueState.phase(RESCUE_FLY_HOME) != lastPhase(RESCUE_MISSION_FLY_WP)` → 모든 I-term/CPA 리셋
5. **✅ 정상 동작**: Phase 전환 리셋이 지연 없이 바로 적용됨.

### 8.4 ⚠️ GPS 노이즈 판별 조건의 문제

```c
// sensorUpdate()에서 A포인트 생성 조건
bool isNoise = (distToHomeM < 20.0f || distToHomeM > 100.0f);
```

- 100m 초과 거리에서 이륙하면 항상 "노이즈"로 판단되어 A포인트가 생성되지 않음
- 긴 활주로나 넓은 공간에서 이륙 시 A포인트 미생성 → Rescue 시 홈으로만 귀환
- **의도된 설계**일 수 있으나, 사용자 설정과 무관하게 강제됨

### 8.5 ⚠️ `setReturnAltitude()`의 호출 주기

`setReturnAltitude()`는 `RESCUE_IDLE` case에서만 호출됨:
```c
switch (rescueState.phase) {
    case RESCUE_IDLE: setReturnAltitude(); break;
    // ...
}
```

**의미**: Rescue가 IDLE 상태일 때만 최대 고도(`maxAltitudeCm`)와 목표 귀환 고도(`returnAltitudeCm`)가 갱신됨. Rescue 활성 중에는 이 값들이 고정됨.

**영향**: Rescue 진입 후 상승/비행 중에 더 높은 고도에 도달해도 `returnAltitudeCm`이 업데이트되지 않음. 이는 의도된 설계이나, 장시간 비행 후 Rescue 발동 시 초기 고도 정보만 사용하게 됨.

### 8.6 ⚠️ `calculateAltitudePitch()` smoothedPitch 리셋 조건

```c
if (!smoothedPitchInitialized || smoothedPitchNeedsReset) {
    smoothedPitch = (midPitch * 100.0f) + (attitude.values.pitch * 10.0f);
    smoothedPitch = constrainf(smoothedPitch, outputMin, outputMax);
    smoothedPitchInitialized = true;
    smoothedPitchNeedsReset = false;
}
```

`smoothedPitchNeedsReset`은 Phase 전환 시 `true`로 설정됨. 리셋 시 현재 attitude pitch + midPitch trim으로 초기화 → Phase 전환 시 부드러운 전환 보장. **✅ 올바른 설계.**

---

## 9. 종합 검증 매트릭스

### 9.1 시나리오 항목별 검증

| # | 시나리오 항목 | 코드 위치 | 상태 | 설명 |
|---|-------------|-----------|------|------|
| 1 | 레스큐 초기 3초 상승 | ATTAIN_ALT timeout 3,000,000µs | ✅ | 정확히 3초 |
| 2 | Fly home: 짝수→A포인트, 홀수→Home | `descentAlt % 2` 조건 분기 | ✅ | 코드 일치 |
| 3 | 이륙 시 A포인트 생성 (짝수) | `sensorUpdate()` → `takeoffVectorCaptured` | ✅ | 20-100m 범위 |
| 4 | Rescue 중 A포인트 생성 (홀수) | FLY_HOME → `!aPointValid && distance <= descentDistance` | ✅ | 거리 기반 |
| 5 | A포인트 통과 후 DESCENT/SHUTTLE | FLY_HOME → `shuttleCount == 0 ? DESCENT : SHUTTLE` | ✅ | CPA 판정 |
| 6 | 셔틀 A/B 왕복 | `handleShuttleProgress()` CPA | ✅ | 20cm hysteresis |
| 7 | 무한셔틀 모든 단계에서 발동 | 각 phase case 내 `aux<1400` 체크 | ✅ | |
| 8 | 미션 웨이포인트 비행 | `mission.c` CPA → WP++ | ✅ | |
| 9 | 모든 WP FLYOVER 처리 | `missionCheckAdvance()` — 타입 미구분 | ✅ | |
| 10 | WP 완료 후 Home 귀환 | `missionStop()` → FLY_HOME | ✅ | |
| 11 | 3가지 모드 상호 전환 | 각 phase 내 Aux 재확인 | ✅ | 매 루프 |
| 12 | **조종권 반환** | 4-layer chain (core→gps→pid→mixer) | ✅ | 확실 |
| 13 | Failsafe Rescue 해제 조건 | `areSticksActive(30)` or switch | ✅ | 의도된 설계 |
| 14 | Waypoint 0개 + Aux 1400-1600 데드락 | INITIALIZE → missionStart() → phase 재설정 | 🐛 | **버그 발견** |
| 15 | 급하강 중 Roll=0 → 방향 이탈 가능성 | isDescentFalling → gpsRescueAngle[ROLL]=0 | ⚠️ | 주의 필요 |
| 16 | LANDING 진입 30m 제한 고정 | `distanceToHomeM <= 30.0f` | ⚠️ | 하드코딩 |

### 9.2 상태 천이 커버리지

```
모든 RescuePhase_e 값: 13개
코드에서 switch 처리: 12개 (COMPLETE만 stop 호출, ABORT는 DO_NOTHING 전환)
미사용: 없음
```

---

## 10. 결론 및 권장사항

### 10.1 발견된 문제점 요약

| 심각도 | 문제 | 설명 |
|--------|------|------|
| 🔴 **높음** | **Waypoint 0개 + Aux 1400-1600 → 데드락** | `missionStart()`가 phase를 재설정하지만, missionIsActive=false로 phase 변경 없음 → RESCUE_INITIALIZE 무한 루프 |
| 🟡 **중간** | **급하강 중 Roll 제어 중단** | `isDescentFalling` 활성 중 고도가 descentAlt+5m 이상이면 Roll=0, Yaw=0, throttle=min → 방향 제어 없이 직진 |
| 🟡 **중간** | **LANDING 진입 30m 고정** | 홈 30m 이내 조건이 하드코딩되어 있어 사용자 설정 불가 |
| 🔵 **낮음** | **missionStop() 후 aPointValid 유지** | missionStop() 주석과 달리 A포인트가 유효하면 FLY_HOME이 A포인트로 비행 |
| 🔵 **낮음** | **A포인트 GPS 노이즈 100m 제한** | 100m 초과 이륙 시 A포인트 미생성 (의도된 설계일 수 있음) |
| 🔵 **낮음** | **setReturnAltitude() IDLE에서만 실행** | Rescue 활성 중에는 returnAltitudeCm 갱신 안 됨 |

### 10.2 시나리오 대비 코드 분석

**시나리오**: "미션비행이 실행되지 않고 조종이 순간적으로 되었다 잃었다를 반복"

**가장 유력한 원인 분석**:

1. **Waypoint 0개에서 Aux 1400-1600**: 위에서 발견된 🔴데드락으로 인해 Rescue가 전혀 실행되지 않음. 조종권이 사용자에게 있는 것처럼 보이지만, 실제로는 Rescue가 INITIALIZE에서 멈춰 있음.

2. **Aux 채널 노이즈 (경계값 1400/1600 근처)**: Aux 값이 노이즈로 인해 1390↔1410으로 변동하면:
   - 1399: `aux < 1400` → SHUTTLE_INFINITE
   - 1401: `aux < 1600` → missionStart() / SHUTTLE_INFINITE 유지
   - 1399로 다시 떨어짐 → phase는 SHUTTLE_INFINITE 유지
   - **1600 경계에서는**: 1599→SHUTTLE? 1601→INITIALIZE→ATTAIN_ALT→FLY_HOME
   - 노이즈로 1599↔1601 변동 시 SHUTTLE_INFINITE ↔ Rescue 전환 반복

3. **Mission → FLY_HOME → Mission 재진입 루프**: WP 완료 후 FLY_HOME → 다음 루프에서 missionStart() → WP 처음부터 → 다시 FLY_HOME... 이 빠르게 반복되면 "조종이 순간적으로 되었다 잃었다"처럼 보일 수 있음 (단, 이는 WP가 있을 때만 해당).

### 10.3 비행 전 필수 확인 사항

1. **Web 기반 시뮬레이션**: 각 시나리오별 Phase 천이를 시뮬레이션하여 데드락 검증
2. **Aux 채널 안정성 확인**: 1400/1600 경계값에서 Aux 채널 노이즈 확인
3. **Waypoint 설정 상태 확인**: Waypoint 0개일 때 Aux 1400-1600 구간에 있지 않은지 확인
4. **Blackbox 로그 분석**: `rescueState.phase`, `isMissionActive`, `currentMissionWpIndex`, `aPointValid`, `takeoffVectorCaptured` 로깅 확인
5. **급하강 시뮬레이션**: isDescentFalling 활성화 조건에서 롤 제어 중단 확인

### 10.4 최종 결론

**코드 수정 없이 분석만 수행한 결과**:

✅ **시나리오 80%+가 코드에 정확히 구현되어 있음** — 특히 3-way Aux 분기, A포인트 생성/추적, CPA 터치 판정, 조종권 반환 4단계 체인은 확실히 검증됨.

🔴 **1개의 높은 심각도 버그 발견** — Waypoint가 0개일 때 Aux 1400-1600에서 RESCUE_INITIALIZE 데드락.

⚠️ **1개의 중간 심각도 이슈 발견** — 급하강 중 방향 제어 중단으로 인한 홈 이탈 가능성.

**"조종이 순간적으로 되었다 잃었다 반복" 증상의 가장 유력한 원인은 Waypoint 0개 + Aux 1400-1600 데드락 또는 Aux 경계값 노이즈로 인한 Phase 전환 반복으로 추정됨.**
