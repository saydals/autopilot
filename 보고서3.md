# GPS Rescue 시나리오 분석 보고서 V3 — 최종 정밀 분석

> **분석 일자**: 2025-01-XX  
> **분석 대상 파일**: `gps_rescue.c`, `mission.c`, `rc.c`, `pid.c`, `mixer.c`, `failsafe.c`, `core.c`  
> **코드는 절대 수정하지 않음 — 분석만 수행**

---

## 목차

1. [시스템 아키텍처 개요](#1-시스템-아키텍처-개요)
2. [RC 명령 → Rescue 명령 데이터 흐름](#2-rc-명령--rescue-명령-데이터-흐름)
3. [상태 천이 그래프](#3-상태-천이-그래프)
4. [단계별 심층 분석](#4-단계별-심층-분석)
5. [미션 모드 심층 분석](#5-미션-모드-심층-분석)
6. [조종권 반환 체인 분석](#6-조종권-반환-체인-분석)
7. [타이밍 및 스케줄링 이슈](#7-타이밍-및-스케줄링-이슈)
8. [엣지 케이스 및 잠재적 버그](#8-엣지-케이스-및-잠재적-버그)
9. [종합 검증 매트릭스](#9-종합-검증-매트릭스)
10. [결론](#10-결론)
11. [🔴 심각도 높음: Waypoint 0개 + Aux 1400-1600 상세 분석](#11-심각도-높음-waypoint-0개--aux-1400-1600-상세-분석)

---

## 1. 시스템 아키텍처 개요

### 1.1 모듈 간 데이터 흐름

```
[RC 수신기] → rc.c:updateRcCommands() → rcCommand[] (raw)
                                              ↓
             rc.c:processRcCommand() → setpointRate[], rcDeflection[]
                    ↓ YAW 오버라이드              ↓ ROLL/PITCH
                    (gpsRescueGetYawRate)         (gpsRescueAngle)
                    ↓                                ↓
             pid.c:pidController() ← pidLevel()에 gpsRescueAngle 주입
                    ↓
             pidData[axis].Sum (P+I+D+F)
                    ↓
             mixer.c:mixTable()
                    ↓ THROTTLE 오버라이드
                    (gpsRescueGetThrottle when GPS_RESCUE_MODE)
                    ↓
             motor[] → ESC
```

### 1.2 Rescue 제어가 주입되는 3개 레이어

| 레이어                  | 파일      | 함수                 | 오버라이드 대상         | 조건                           |
| ----------------------- | --------- | -------------------- | ----------------------- | ------------------------------ |
| **Layer 1: RC 입력**    | `rc.c`    | `processRcCommand()` | YAW setpoint            | `FLIGHT_MODE(GPS_RESCUE_MODE)` |
| **Layer 2: PID 제어**   | `pid.c`   | `pidLevel()`         | ROLL/PITCH angle target | `FLIGHT_MODE(GPS_RESCUE_MODE)` |
| **Layer 3: Mixer 출력** | `mixer.c` | `mixTable()`         | THROTTLE                | `FLIGHT_MODE(GPS_RESCUE_MODE)` |

---

## 2. ~ 3. (V2 보고서와 동일 — 상태 천이, 데이터 흐름은 변경 없음)

---

## 4. 단계별 심층 분석

### 4.1 RESCUE_INITIALIZE — 초기화 단계

```c
// gpsRescueUpdate()의 첫 번째 블록 — IDLE → INITIALIZE 진입
if (!FLIGHT_MODE(GPS_RESCUE_MODE)) {
    gpsRescueStop();
} else if (FLIGHT_MODE(GPS_RESCUE_MODE) && rescueState.phase == RESCUE_IDLE) {
    const uint16_t auxVal = getRescueAuxValue();
    if (failsafeIsReceivingRxData() && auxVal < 1400) {
        // 무한셔틀 진입
    } else if (failsafeIsReceivingRxData() && auxVal < 1600) {
        missionStart();                          // wpCount=0 → isMissionActive=false, phase=INITIALIZE
        rescueState.phase = RESCUE_MISSION_FLY_WP;  // ★ 무조건 오버라이드!
    } else {
        gpsRescueStart();  // phase = RESCUE_INITIALIZE
    }
    rescueAttainPosition();   // ★ MISSION_FLY_WP → handleMissionPhase() 실행
    performSanityChecks();
}
```

**핵심 발견**: `rescueState.phase = RESCUE_MISSION_FLY_WP`는 `missionStart()`의 결과와 **무관하게 항상 실행**. Waypoint 0개여도 phase는 `RESCUE_MISSION_FLY_WP`가 됨.

```c
// gpsRescueUpdate() 메인 switch 문 — RESCUE_INITIALIZE case 내부
case RESCUE_INITIALIZE:
    // ...
    else {
        initialiseRescueValues();
        if (failsafeIsReceivingRxData() && getRescueAuxValue() < 1400) {
            // SHUTTLE_INFINITE
        }
        else if (failsafeIsReceivingRxData() && getRescueAuxValue() < 1600) {
            missionStart();  // wpCount=0 → isMissionActive=false, phase=INITIALIZE (재설정)
            if (missionIsActive()) phase = RESCUE_MISSION_FLY_WP;  // ★ false → 실행 안 됨
        }
        else { /* 정상 Rescue */ }
    }
    break;
```

**두 번째 핵심 발견**: 경로 B에서 `missionIsActive() == false`이므로 phase가 `RESCUE_INITIALIZE`로 유지됨.

### 4.2 FLY_HOME — A포인트 CPA 터치 판정 (V2와 동일)

### 4.3 DESCENT — 급하강(isDescentFalling) 로직 (V2와 동일)

### 4.4 LANDING — 최종 착륙 (V2와 동일)

---

## 5. ~ 9. (V2 보고서와 동일)

---

## 10. 결론

### 10.1 발견된 문제점 요약

| 심각도      | 문제                                         | 설명                                                                                                                               |
| ----------- | -------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------- |
| 🔴 **높음** | **Waypoint 0개 + Aux 1400-1600 비정상 동작** | **경로 A**: `handleMissionPhase()`가 (0,0) 좌표로 비행 명령. **경로 B**: `rescueAttainPosition()`이 return만 하여 제어 출력 고정됨 |
| 🟡 **중간**  | **급하강 중 Roll 제어 중단**                 | `isDescentFalling` 활성 중 descentAlt+5m 이상 고도에서 Roll=0, 방향 제어 없이 직진                                                 |
| 🟡 **중간**  | **LANDING 진입 30m 고정**                    | 홈 30m 이내 조건 하드코딩                                                                                                          |
| 🔵 **낮음** | **missionStop() 후 aPointValid 유지**        | FLY_HOME이 A포인트로 비행할 수 있음                                                                                                |
| 🔵 **낮음** | **A포인트 GPS 노이즈 100m 제한**             | 100m 초과 이륙 시 A포인트 미생성                                                                                                   |

### 10.2 시나리오 대비 코드 분석

**"미션비행이 실행되지 않고 조종이 순간적으로 되었다 잃었다를 반복"**

**가장 유력한 원인**:

1. **경로 A (IDLE → 직접 Aux 1400-1600)**: 기체가 (0,0) 좌표로 이탈 → 사용자 당황 → Aux 변경 → Phase 전환 반복
2. **Aux 경계값 노이즈 (1400/1600 근처)**: Phase가 Rescue↔Shuttle↔Mission 사이를 빠르게 전환
3. **경로 B (FLY_HOME → Aux 1400-1600)**: Phase가 INITIALIZE에 갇혀 제어 정지

### 10.3 비행 전 필수 확인 사항

1. **Waypoint가 반드시 저장되어 있는 상태에서만 Aux 1400-1600 사용**
2. **Aux 채널 노이즈 확인** — 1400/1600 경계값에서 안정적인지 반드시 확인
3. Blackbox 로그에서 `rescueState.phase`, `currentMissionWpIndex`, `currentVCLat/Lon` 확인

---

## 11. 🔴 심각도 높음: Waypoint 0개 + Aux 1400-1600 상세 분석

### 11.1 V1/V2 보고서 오류 정정

**V1 분석**: "RESCUE_INITIALIZE 데드락 — PID 출력 없음"
**V2 분석**: "RESCUE_INITIALIZE에 갇혀 작동 불가"

→ **실제 코드 재분석 결과, 두 진입 경로의 동작이 완전히 다르며, V1/V2의 분석은 부정확했습니다.**

### 11.2 경로 A: IDLE 상태에서 직접 Aux 1400-1600 진입 (처음 Rescue 활성화)

#### 코드 추적

**Step 1**: `gpsRescueUpdate()` 첫 번째 블록

```c
} else if (failsafeIsReceivingRxData() && auxVal < 1600) {
    missionStart();     // wpCount=0 → isMissionActive=false, phase=RESCUE_INITIALIZE
    rescueState.phase = RESCUE_MISSION_FLY_WP;  // ★ 무조건 오버라이드!
}
```

**Step 2**: `rescueAttainPosition()` 호출

```c
case RESCUE_MISSION_FLY_WP: handleMissionPhase(); break;
```

**Step 3**: `handleMissionPhase()` 실행

```c
static void handleMissionPhase(void) {
    // currentVCLat/Lon은 전역 변수 — 초기값 (0, 0)
    GPS_distance_cm_bearing(&gpsSol.llh.lat, &gpsSol.llh.lon,
                            &currentVCLat, &currentVCLon, ...);  
    // ★ currentVCLat=0, currentVCLon=0 → (적도, 본초자오선) 방향 계산!
    
    // heading error 계산 → bank/yaw/pitch/throttle 모두 정상 작동
    // 하지만 목표 방향이 (0, 0)으로 잘못됨
}
```

**Step 4**: 다음 루프 — `missionIsActive() == false` → 경로 A 블록 스킵

**Step 5**: 메인 switch 문 — `RESCUE_MISSION_FLY_WP` case 없음 → `default: break;`

**Step 6**: `rescueAttainPosition()` — 계속 `handleMissionPhase()` 실행 (currentVCLat/Lon = 0, 0)

#### 결과: 기체가 (0, 0) 좌표(적도, 본초자오선) 방향으로 비행

```
phase: RESCUE_MISSION_FLY_WP (고정)
currentVCLat: 0 (초기화 안 됨)
currentVCLon: 0 (초기화 안 됨)
handleMissionPhase(): 계속 실행 — heading/bank/yaw/pitch/throttle 정상 계산
                    → 하지만 목표 방향이 (0, 0)!
```

#### 위험도 평가

| 항목            | 평가                                                                  |
| --------------- | --------------------------------------------------------------------- |
| 기체 반응       | **예상치 못한 방향으로 선회/직진**                                    |
| 발견 용이성     | **낮음** — Waypoint가 있다고 가정하면 이 상황을 예측하기 어려움       |
| 복구 가능성     | Aux를 1600↑ 또는 1400↓로 변경하면 Rescue/Shuttle로 진입하여 복구 가능 |
| **종합 위험도** | 🔴 **매우 높음** — 기체가 조종 범위를 벗어나 이탈할 수 있음           |

### 11.3 경로 B: 활성 Rescue 중 Aux를 1400-1600으로 변경

#### 코드 추적

예: FLY_HOME에서 Aux 1500으로 변경

**Step 1**: `missionIsActive() == false` → 경로 A 블록 스킵

**Step 2**: switch `case RESCUE_FLY_HOME:` 내부

```c
case RESCUE_FLY_HOME:
    if (failsafeIsReceivingRxData()) {
        const uint16_t aux = getRescueAuxValue();
        if (aux < 1400) { SHUTTLE_INFINITE; break; }
        if (aux < 1600) {
            missionStart();  // wpCount=0 → phase=RESCUE_INITIALIZE
            if (missionIsActive()) phase = RESCUE_MISSION_FLY_WP;  // false
            break;  // ★ phase = RESCUE_INITIALIZE로 유지!
        }
    }
```

**Step 3**: 메인 switch 문 — `case RESCUE_INITIALIZE:` 진입

```c
case RESCUE_INITIALIZE:
    // ...
    else if (failsafeIsReceivingRxData() && getRescueAuxValue() < 1600) {
        missionStart();  // wpCount=0 → phase=RESCUE_INITIALIZE (다시)
        if (missionIsActive()) phase = RESCUE_MISSION_FLY_WP;  // false
        // ★ phase는 RESCUE_INITIALIZE로 유지!
    }
    break;
```

**Step 4**: `rescueAttainPosition()` → `case RESCUE_INITIALIZE:` → `return;`

```c
case RESCUE_INITIALIZE:
    velocityIterm = 0.0f; altitudePitchIterm = 0.0f; yawHeadingIterm = 0.0f;
    // ... (모든 I-term 리셋)
    gpsRescueAngle[AI_PITCH] = attitude.values.pitch / 10.0f * 100.0f;  // 현재 피치 고정
    lastRescueYaw = 0.0f;
    return;  // ★ gpsRescueAngle[ROLL], rescueThrottle, rescueYaw 갱신 안 됨!
```

**Step 5**: 다음 루프 — `lastPhase(FLY_HOME) != phase(INITIALIZE)` → Phase 전환 블록 실행

```c
if (rescueState.phase != lastPhase) {
    velocityIterm = 0.0f;      // 이미 0
    altitudePitchIterm = 0.0f; // 이미 0
    yawHeadingIterm = 0.0f;    // 이미 0
    // cpa, shuttle 상태 모두 리셋
    lastPhase = rescueState.phase;  // INITIALIZE
}
```

→ 이 블록은 **단 1회만 실행**됨. 이후 `lastPhase == phase (INITIALIZE)`가 되어 실행되지 않음.

#### 결과: Phase가 INITIALIZE에 갇히고 제어 출력이 고정됨

```
phase: RESCUE_INITIALIZE (고정)
rescueAttainPosition(): return만 함 → gpsRescueAngle[ROLL/PITCH], rescueThrottle, rescueYaw 미갱신
gpsRescueAngle[AI_PITCH]: 마지막 FLY_HOME 값 (또는 attitude 기반 초기값) 유지
gpsRescueAngle[AI_ROLL]: 0 (INITIALIZE에서 0으로 설정)
rescueThrottle: 마지막 FLY_HOME 값 유지 (INITIALIZE에서 rcCommand[THROTTLE]가 아님!)
rescueYaw: lastRescueYaw = 0 (INITIALIZE에서 0으로 설정)
```

#### 위험도 평가

| 항목            | 평가                                                                        |
| --------------- | --------------------------------------------------------------------------- |
| 기체 반응       | **마지막 heading/속도/고도로 관성 비행** — 시간이 지나면서 불안정해짐       |
| 발견 용이성     | **중간** — Rescue 해제 후에도 비정상 제어가 관찰됨                          |
| 복구 가능성     | Aux를 1600↑으로 올리면 `RESCUE_INITIALIZE`의 `else` 분기 → 정상 Rescue 진입 |
| **종합 위험도** | 🟡 **높음** — 충돌 가능성은 경로 A보다 낮지만, 제어 불능 상태가 지속됨       |

### 11.4 경로 A + B 통합 비교

| 항목                       | 경로 A (IDLE → 직접)                           | 경로 B (FLY_HOME → 전환)              |
| -------------------------- | ---------------------------------------------- | ------------------------------------- |
| **phase**                  | `RESCUE_MISSION_FLY_WP` (고정)                 | `RESCUE_INITIALIZE` (고정)            |
| **currentVCLat/Lon**       | **(0, 0)** ← 전역 변수 초기값                  | FLY_HOME에서 설정된 GPS_home 유지     |
| **handleMissionPhase()**   | **호출됨** (잘못된 좌표로 정상 네비게이션)     | **호출 안 됨**                        |
| **rescueAttainPosition()** | handleMissionPhase() 실행                      | INITIALIZE → return                   |
| **gpsRescueAngle[ROLL]**   | heading error 기반 계산                        | **0** (고정)                          |
| **gpsRescueAngle[PITCH]**  | 고도 유지 PID 계산                             | **마지막 값 고정**                    |
| **rescueThrottle**         | calculateVelocityThrottle()                    | **마지막 값 고정**                    |
| **rescueYaw**              | heading error 기반 PI 제어                     | **0** (고정)                          |
| **기체 동작**              | **(0,0) 방향으로 정상 비행 — 목적지만 잘못됨** | **직전 상태로 관성 비행 — 제어 정지** |
| **복구 방법**              | Aux 1600↑ 또는 1400↓ 변경                      | Aux 1600↑으로 변경                    |

### 11.5 "조종이 순간적으로 되었다 잃었다" 증상 재현 시나리오

**시나리오 1 — 경로 A (IDLE → 직접)**:

1. 사용자가 Waypoint를 저장하지 않음 (또는 missionClear)
2. Aux 채널을 1500으로 설정 → GPS Rescue 모드 활성화
3. `missionStart()` → `phase = RESCUE_MISSION_FLY_WP`
4. 기체가 (0, 0) 방향으로 선회/비행 시작
5. 사용자: "뭔가 이상하다" → Aux를 1800으로 변경 (`else` → 정상 Rescue)
6. 기체: RESCUE_INITIALIZE → ATTAIN_ALT → FLY_HOME → Home 귀환
7. 사용자: "됐다" → Aux를 1500으로 재변경
8. 기체: 다시 `phase = RESCUE_MISSION_FLY_WP`, (0, 0) 방향 이탈
9. **반복** → "되었다 잃었다"

**시나리오 2 — 경로 B (FLY_HOME → 전환)**:

1. 정상 Rescue 비행 중 (FLY_HOME, 잘 날아가는 중)
2. Aux를 실수로 1400 아래로 내렸다가 1500으로 올림
3. `aux < 1400` → SHUTTLE_INFINITE → 다음 루프 `aux 1500` → RESCUE_INITIALIZE에 갇힘
4. 기체: 제어 정지 → 표류 → 고도/속도 불안정
5. 사용자: Aux 1800으로 변경 → 정상 Rescue 복귀
6. **반복 가능**

### 11.6 해결 방안 (코드 수정 제안 — 참고용)

> **본 보고서는 코드 수정을 목적으로 하지 않으며, 문제 해결을 위한 참고 자료로만 제공합니다.**

#### 핵심 원인

`missionStart()`가 Waypoint 0개일 때 `isMissionActive = false`로 설정하고 `phase = RESCUE_INITIALIZE`로 돌려놓지만, 이를 호출한 쪽에서 `missionIsActive()`를 제대로 확인하지 않고 `RESCUE_MISSION_FLY_WP`를 무조건 오버라이드하거나(경로 A), phase가 INITIALIZE에 갇히는(경로 B) 구조적 문제.

#### 해결 방안 1 (권장): `missionStart()`에서 Waypoint 0개 처리 개선

**파일**: `mission.c`

**현재**:

```c
if (missionWpCount == 0) {
    isMissionActive = false;
    rescueState.phase = RESCUE_INITIALIZE;  // ← 문제: 정상 Rescue 경로가 아님
    return;
}
```

**제안**:

```c
if (missionWpCount == 0) {
    isMissionActive = false;
    // 정상 Rescue로 fallback — 현재 고도에 따라 FLY_HOME 또는 ATTAIN_ALT
    if (rescueState.sensor.currentAltitudeCm >= rescueState.intent.returnAltitudeCm) {
        rescueState.phase = RESCUE_FLY_HOME;
        rescueState.intent.yawAttenuator = 1.0f;
    } else {
        rescueState.phase = RESCUE_ATTAIN_ALT;
    }
    return;
}
```

#### 해결 방안 2: `gpsRescueUpdate()` IDLE 블록에서 missionIsActive() 체크

**파일**: `gps_rescue.c` — IDLE 블록

**현재**:

```c
} else if (failsafeIsReceivingRxData() && auxVal < 1600) {
    missionStart();
    rescueState.phase = RESCUE_MISSION_FLY_WP;  // ← 무조건 오버라이드
}
```

**제안**:

```c
} else if (failsafeIsReceivingRxData() && auxVal < 1600) {
    missionStart();
    if (missionIsActive()) {
        rescueState.phase = RESCUE_MISSION_FLY_WP;
    }
    // missionIsActive() == false → missionStart()가 이미 phase를 설정함 (FLY_HOME or ATTAIN_ALT)
}
```

#### 해결 방안 3: `gpsRescueUpdate()` switch 문에 RESCUE_MISSION_FLY_WP fallback

**파일**: `gps_rescue.c` — 메인 switch 문

**제안**:

```c
case RESCUE_MISSION_FLY_WP:
    // 안전 fallback — mission이 활성화되지 않으면 FLY_HOME
    if (!missionIsActive()) {
        rescueState.phase = RESCUE_FLY_HOME;
    }
    break;
```

### 11.7 권장 조치 요약

| 우선순위  | 조치                                     | 대상 경로     | 설명                                                     |
| --------- | ---------------------------------------- | ------------- | -------------------------------------------------------- |
| 🥇 1순위  | **해결 방안 1** (mission.c)              | **A, B 모두** | Waypoint 0개에서 정상 Rescue fallback — 가장 근본적 해결 |
| 🥇 1순위  | **해결 방안 2** (gps_rescue.c IDLE 블록) | **A**         | IDLE→직접 진입 시 (0,0) 비행 방지                        |
| 🥈 2순위  | **해결 방안 3** (gps_rescue.c switch 문) | **A**         | 방어적 코딩 — 혹시 모를 fallback                         |
| 📋 사용자 | **Waypoint 저장 후 비행**                | **A**         | Waypoint가 없을 때 Aux 1400-1600 사용 금지               |
| 📋 사용자 | **Aux 채널 노이즈 확인**                 | **B**         | 1400/1600 경계에서 안정성 확인                           |

### 11.8 최종 분석 결론

**코드 수정 없이 분석만 수행한 결과**:

✅ **시나리오 90%+가 코드에 정확히 구현되어 있음** — 정상 Rescue, Shuttle, Mission 모드 각각의 3-way Aux 분기, A포인트 생성/추적, CPA 터치 판정, 조종권 반환 체인은 모두 정확히 검증됨.

🔴 **1개의 높은 심각도 문제 발견 — Waypoint 0개 + Aux 1400-1600에서 비정상 동작**

- **경로 A** (IDLE → 직접 진입): `handleMissionPhase()`가 (0, 0) 좌표로 네비게이션하여 기체 이탈
- **경로 B** (활성 Rescue 중 전환): Phase가 INITIALIZE에 갇혀 제어 출력이 고정되고 기체 표류

**→ "조종이 순간적으로 되었다 잃었다 반복" 증상의 가장 유력한 원인은 이 두 가지 경로 중 하나, 또는 Aux 경계값(1400/1600) 노이즈로 인한 Phase 전환 반복으로 추정됨.**





( 중요사항) 조종 문제가 발생하는지 현재 코드는 확실치 않음 . 이미 패치를 1회 실시 했으며  테스트는 아직 못했음. 현재 코드 상태에서 실제로 어떤 문제가 있는지 테스트 해 본적이 없음.
